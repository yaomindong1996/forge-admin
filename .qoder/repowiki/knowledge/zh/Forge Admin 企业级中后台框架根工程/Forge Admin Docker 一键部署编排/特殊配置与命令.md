进入对应目录后执行：`cp .env.example .env` 修改密码与端口，再 `docker compose up -d` 启动；访问 `http://localhost/forge`，默认账号 admin/123456。生产环境需把 `application-datasource.yml` 与 `../forge-server/db/migration` 放在 `docker-forge-admin/` 同级位置以便 volume 挂载。