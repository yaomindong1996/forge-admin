通过 Maven 打包为 `forge-admin-server.jar` 或 `forge-admin-server.war`；Flyway 默认从 `./db/migration`、`../db/migration`、`forge-server/db/migration` 三个文件系统路径加载脚本，可通过 `FORGE_FLYWAY_ENABLED` 环境变量关闭；MCP 服务端默认关闭，需设置 `FORGE_MCP_ENABLED=true` 并在 `/mcp` 暴露。