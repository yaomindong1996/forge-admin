客户端通过 `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` 声明 `FlowClientAutoConfiguration` 和 `FlowClientHelperAutoConfiguration`，引入 `forge-flow-client` 依赖后自动生效；远程 Job 执行需配置 `forge.flow.job.remote.enabled=true` 才会注册 `RemoteJobFlowExecutor`。