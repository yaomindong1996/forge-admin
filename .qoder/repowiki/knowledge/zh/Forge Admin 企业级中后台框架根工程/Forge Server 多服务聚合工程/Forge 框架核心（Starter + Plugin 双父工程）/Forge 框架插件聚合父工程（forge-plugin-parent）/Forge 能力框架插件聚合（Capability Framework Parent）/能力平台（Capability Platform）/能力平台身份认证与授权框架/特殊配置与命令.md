通过 `forge.capability.identity.enabled` 开关启用；issuer/resource/openapi-resource 必须为 HTTPS（仅 localhost HTTP 开发允许）；Access Token TTL 限制 1~15 分钟、授权码 TTL ≤ 120 秒、last_used_at 节流间隔 ≤ 5 分钟，均由 `CapabilityIdentityProperties` 的 validated* 方法在启动时强制校验。