模块通过 `pom.xml` 显式声明对 `forge-starter-*` 系列 starter 的依赖，其中 `forge-plugin-job` 设为 `<optional>true</optional>`，使不含调度器的服务（如 app-server）引入本插件时不会被强制拉起 Quartz；`CollaborationSyncJobManager` 使用 `@ConditionalOnClass(ISysJobConfigService.class)` 进一步避免在无调度环境装配 Bean。