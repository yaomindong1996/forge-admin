通过 Maven 父 POM `forge-plugin-capability-parent` 管理版本，模块自身无独立构建脚本；Spring Boot 自动装配由 `src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` 声明，无需手动配置即可启用能力注册中心。