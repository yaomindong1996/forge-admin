启用需设置 `spring.ai.mcp.server.enabled=true`，且协议必须为 `STREAMABLE`、API 类型为 `SYNC`，否则 `ForgeMcpProtocolGuard` 构造时抛出 `IllegalStateException`；可通过实现 `McpRequestLifecycle` 和 `McpToolContributor` 接口自定义请求生命周期与新增 MCP 工具。