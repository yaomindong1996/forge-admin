Spring Boot + MyBatis-Plus（分页 Page/PageQuery）、Sa-Token（`@SaCheckPermission` 权限控制）、Lombok、Jackson（JsonNode 策略解析）、Forge Starter 提供的 `RespInfo`、`OperationLog`、`SessionHelper`、`ApiEncrypt`/`ApiDecrypt` 等通用注解。