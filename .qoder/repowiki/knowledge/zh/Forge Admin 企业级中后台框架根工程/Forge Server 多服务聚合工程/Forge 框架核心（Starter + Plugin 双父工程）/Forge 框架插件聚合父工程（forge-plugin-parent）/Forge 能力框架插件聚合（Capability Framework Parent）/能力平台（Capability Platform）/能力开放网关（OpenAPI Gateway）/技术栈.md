Spring Boot 自动装配 + Lombok；认证使用 Sa-Token 白名单放行后由自定义 `OpenGatewayAuthenticator` 处理 Bearer/OAuth 与 HMAC-SHA256 签名；限流/幂等/防重放复用 `forge.starter.openapi.security` 通用组件；持久化通过 MyBatis Mapper；定时任务基于 `forge.starter.job` 的 `IJobExecutor` 与 `@JobHandler`。