Spring Boot + MyBatis-Plus（`ServiceImpl`/`LambdaQueryWrapper`/分页）+ Hutool + Lombok；消息通道通过 `forge-starter-message` 的 `MessageClient`/`MessageChannel`/`MessageTemplateEngine` 抽象；多租户通过 `TenantContextHolder` 注入；事务后异步投递使用 Spring `@TransactionalEventListener(AFTER_COMMIT)`。