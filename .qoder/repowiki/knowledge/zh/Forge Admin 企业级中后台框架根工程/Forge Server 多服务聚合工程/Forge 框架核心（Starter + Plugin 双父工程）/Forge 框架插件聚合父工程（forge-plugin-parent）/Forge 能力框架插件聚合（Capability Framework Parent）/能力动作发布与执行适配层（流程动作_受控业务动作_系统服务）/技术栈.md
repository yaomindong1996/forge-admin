Spring Boot 自动装配（`@AutoConfiguration` + `AutoConfiguration.imports`）、MyBatis Mapper XML、Jackson `ObjectNode`/`ArrayNode` 动态构建 JSON Schema（遵循 `CapabilitySchemaValidator.DRAFT_2020_12`）、Lombok `@RequiredArgsConstructor` 构造注入、Apache Commons Lang3 字符串处理。