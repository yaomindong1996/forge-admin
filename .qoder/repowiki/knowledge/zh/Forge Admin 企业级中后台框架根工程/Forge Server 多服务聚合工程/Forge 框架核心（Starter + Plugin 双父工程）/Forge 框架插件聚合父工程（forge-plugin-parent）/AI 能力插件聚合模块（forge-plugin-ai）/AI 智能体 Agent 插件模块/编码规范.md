- 每个功能域按 controller/domain/dto/service/mapper 分层组织，引擎内部再按 context/tool/event/persistence 等横切关注点拆分子包。
- 对外交互统一通过 REST Controller 暴露，引擎内部运行态通过 DTO 与 Event 对象解耦请求与流式响应。
- 可扩展能力（工具、权限、事件）采用 SPI 风格：定义接口（如 AgentTool、PermissionEngine、AgentEventListener）并由 Registry/Manager 集中装配。