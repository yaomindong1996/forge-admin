通过 `forge.capability.high-risk.enabled=true` 启用；`flowModelKey` 必须等于 `HighRiskApprovalFlowDefinition.MODEL_KEY`（默认 `forge_capability_high_risk_approval`），否则启动即抛 `HIGH_RISK_FLOW_MODEL_KEY_INVALID`；`crypto.keys` 需配置至少一个密钥 ID 到密钥值的映射，且 `activeKeyId` 指向当前活跃密钥。