- 所有可插拔组件（解析器、分块器、向量存储）均通过 XxxRegistry/XxxFactory 集中注册并按类型路由，新增实现只需注册即可被上层发现。
- 领域对象统一使用 MyBatis-Plus 的 Entity + Mapper 分层，DTO 与请求对象在 service/dto 下独立定义，避免与持久化模型混用。
- 检索流程采用 Pipeline + Handler 链式组合，每个 Handler 聚焦单一步骤（向量检索、BM25、融合、重排、最终化），便于按 searchType 动态编排。