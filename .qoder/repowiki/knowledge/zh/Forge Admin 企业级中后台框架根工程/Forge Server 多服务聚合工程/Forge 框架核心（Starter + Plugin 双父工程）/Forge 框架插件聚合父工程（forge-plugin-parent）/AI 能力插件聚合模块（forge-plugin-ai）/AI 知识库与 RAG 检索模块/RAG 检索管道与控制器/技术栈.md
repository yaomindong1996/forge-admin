Spring Boot + Lombok；检索后端基于 Milvus 稀疏向量 BM25 与稠密向量检索（通过 `VectorStoreFactory`/`VectorStoreService`）；重排通过 `AiModelAdapterRegistry.getRerank()` 动态接入第三方 rerank 模型；查询补全使用 Spring AI ChatModel；结果融合采用 RRF（k=60）与加权求和两种策略。