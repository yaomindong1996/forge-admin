每个 starter 均在 `resources/sql/` 或 `sql/` 目录下提供建表 DDL（如 `config_group_table.sql`、`datascope_tables.sql`、`tenant_migration.sql`），需在应用启动前执行；`forge-starter-datascope` 还附带 `application-datascope-example.yml` 示例配置。