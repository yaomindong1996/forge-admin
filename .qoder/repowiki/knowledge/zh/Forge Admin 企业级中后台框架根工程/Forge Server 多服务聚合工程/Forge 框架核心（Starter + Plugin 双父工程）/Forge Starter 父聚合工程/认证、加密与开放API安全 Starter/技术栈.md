Sa-Token（JWT + Redis/Jackson 扩展）+ Hutool Captcha；加密使用 BouncyCastle (`bcprov-jdk15to8`) 与 Hutool-crypto，支持 SM4/AES/AES-GCM；开放 API 安全基于 Redisson `RRateLimiter` 与分布式锁；Spring Boot 3 自动配置通过 `AutoConfiguration.imports` 注册。