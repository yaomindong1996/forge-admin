运行前需安装 `mysql`、`mysqldump`、`node`、`rg` 工具；`init-db.sh` 默认连接 `127.0.0.1:3306` 的 `forge_admin` 库，密码可通过环境变量或 `--password` 传入；`dump-safe-full.sh` 推荐通过 `MYSQL_PWD` 注入密码以避免命令行泄露；社区版导出入口为 `./export-community-db.sh [--dry-run]`。