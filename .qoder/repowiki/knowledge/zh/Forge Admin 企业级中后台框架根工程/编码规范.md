- 后端所有数据库结构变更以 Flyway 版本化 SQL 脚本形式集中在 forge/db/migration，禁止直接修改已执行脚本
- 前后端配置分离：示例配置放在 *.example.yml，本地敏感配置不入库，通用配置使用占位符并在示例文件中同步更新
- 前端各子应用统一使用 pnpm 管理依赖，并通过 .env.* 文件区分开发与生产环境变量
- AI 相关能力（智能体、Prompt、Skill）集中存放在 code-copilot 与 agents_skills 目录，通过 SKILL.md 与 references 约束行为