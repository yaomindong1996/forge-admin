Vue 3 `<script setup>` + Naive UI（NButton/NModal/n-data-table/n-tabs 等），统一通过 `@/components/ai-form` 的 `AiCrudPage` 声明式构建 CRUD；图标使用 Material Symbols 类名；SSO 通过 `@/api/auth-sso` 的 `createSsoTicket` 实现；字典数据通过 `useDict` composable 注入。