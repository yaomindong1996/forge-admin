web-content-fetcher 首次使用需执行 `pip install scrapling html2text`（系统 Python 下可能需要 `--break-system-packages` 或使用 venv），并通过 `python3 <SKILL_DIR>/scripts/fetch.py <url> [--stealth] [--json] [max_chars]` 调用；其他两个 Skill 无额外构建步骤，仅要求先阅读 AGENTS.md 与 code-copilot 规则。