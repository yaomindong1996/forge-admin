通过 pnpm 安装依赖后，使用 `pnpm dev:h5` 启动 H5 开发服务器（默认端口 3009，由 .env 中 VITE_HTTP_PORT 控制），`pnpm build:h5` 构建生产产物；支持多端命令如 dev:mp-weixin、build:mp-alipay 等；开发时通过 vite.config.js 的 proxy 将 /dev-api 代理到 VITE_HTTP_PROXY_TARGET（默认 http://127.0.0.1:8581/），/ws 走 WebSocket 代理，flow 相关接口额外代理到 VITE_FLOW_PROXY_TARGET。