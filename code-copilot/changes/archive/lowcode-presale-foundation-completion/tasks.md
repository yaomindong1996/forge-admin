# 低代码表单移动扫码与动态字段任务

## A. 规则与发布快照

- [x] A1 增加互斥的显示/隐藏效果配置并兼容旧 `hidden` 规则。
- [x] A2 发布编译保留带运行可见性规则的静态隐藏字段，并透传运行规则/隐藏属性。
- [x] A3 补充前端和后端回归测试。
- [x] A4 兼容旧 `props.__events` 显示/隐藏配置，并贯通设计器预览、保存和发布运行路径。
- [x] A5 运行态表单编译保留静态隐藏字段基线，避免带可见性规则的字段丢失初始状态。

## B. H5 扫码字段

- [x] B1 新增 `barcodeScanner` 字段协议、设计器组件和默认属性。
- [x] B2 实现浏览器摄像头扫码适配，清理媒体流并复用统一扫码错误码。
- [x] B3 接入 `AiFormItem`、`AiForm` 和子表行，扫码后只更新当前字段并分发 `SCAN_COMPLETE`。
- [x] B4 子表补齐扫码上下文，落实“禁止手工输入”配置并避免扫码按钮重复显示。
- [x] B5 将 `barcodeScanner` 纳入业务表单设计器运行字段白名单和字段默认映射。

## C. 子表事件与动态选项

- [x] C1 子表运行时字段统一走 `AiFormItem`，补齐当前行事件上下文。
- [x] C2 增加 `CURRENT_CHILDREN` 受控选项源并隔离行级异步状态。
- [x] C3 增加事件、扫码、选项源纯函数及组件回归测试。
- [x] C4 子表按关系读取字段事件，隐藏字段不参与行级必填校验。

## D. 事务门禁与验证

- [x] D1 增加发布动作 `ASSERT_RECORD` 步骤及状态/归属校验。
- [x] D2 将可退数量等上下界约束纳入同一原子调整 SQL/执行器。
- [x] D3 执行增量测试、构建、静态检查并回填记录。
