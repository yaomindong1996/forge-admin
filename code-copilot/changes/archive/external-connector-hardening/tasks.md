# 外部连接器与数据集运行时加固任务

## 1. SDD 基线

- [x] 盘点外部连接器、数据集、持久化加密、缓存与出站安全现状。
- [x] 明确复用边界和平台级需求。
- [x] 建立 `spec.md`、`tasks.md`、`test-spec.md`、`execution-log.md`。

## 2. 外部连接器运行策略

- [x] 增加统一敏感数据脱敏器。
- [x] 接入权限检查并校验权限配置。
- [x] 接入按租户/API 隔离的分布式限流。
- [x] 接入 GET 成功响应缓存与安全缓存 Key。
- [x] 接入幂等方法安全重试及边界归一。
- [x] 复用统一出站客户端和 `EXTERNAL_CONNECTOR` 场景。
- [x] 增加 `EXTERNAL_CONNECTOR` 出站场景字典迁移。
- [x] 删除 Controller 和运行日志中的敏感原值输出。

## 3. 密钥保护

- [x] 外部系统新增/编辑统一委托 Service，写入持久化密文。
- [x] 编辑空值/掩码保留原凭据。
- [x] 管理查询返回固定掩码，不返回明文或密文。
- [x] 运行调用使用解密后的内存副本。
- [x] 补充存量明文兼容和安全告警。

## 4. 数据集运行时

- [x] 最大行数和超时使用稳健默认及上下界。
- [x] JDBC Connection 设置只读并使用 try-with-resources。
- [x] 查询异常显式传播。
- [x] 日志只记录参数键和 SQL 摘要。
- [x] 接入成功结果缓存和安全缓存 Key。

## 5. 脚本适配器

- [x] JavaScript Engine 缺失时返回明确业务错误。
- [x] 归一脚本执行失败语义。

## 6. 测试与验收

- [x] 读取自动化测试标准并复用本变更基线。
- [x] 增加外部连接器单元测试。
- [x] 增加数据集运行时单元测试。
- [x] 执行定向测试与模块构建。
- [x] 将命令、结果、警告、跳过项和服务清理写入 `execution-log.md`。
- [x] 完成 Spec 合规与代码质量自审。
