# 增量测试计划

## 2026-08-15

本轮按已提交任务增量验证，不重新执行全量端到端流程。

| 范围 | 验证 | 通过标准 |
| --- | --- | --- |
| Task 11 节点模板 | Vitest、ESLint、前端生产构建 | 开始/动作模板可预填且仍可编辑；原有节点配置回归通过 |
| Task 12 旧入口收口 | Vitest、ESLint、前端生产构建 | 触发器和旧流程绑定均显示迁移提示、隐藏写入命令并携带应用上下文跳转 |
| Task 13 对象流程查询 | 目标 Maven 模块测试 | 路由和权限固定；查询以租户、活动应用/对象和逻辑删除条件收口；ID 为字符串且从节点数组提取真实开始节点 |

不启动本地服务：本轮没有可用的本地 MySQL、Redis 和 Flowable 验证环境，因此未执行加密真实调用、权限 403 或数据库 `JSON_TABLE` 查询。前端 API 已在 Task 3 引用目标路由；后端以 Mapper、Service 和控制器契约测试覆盖协议。

Task 10 不纳入通过范围。其运行时前提尚未实现，详见 `tasks.md` 的 blocked 记录。
