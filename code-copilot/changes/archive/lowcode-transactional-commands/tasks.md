# 低代码事务型业务命令任务

## 1. SDD 与差距审计

- [x] 盘点现有 BusinessAction、动态 CRUD、幂等日志、发布快照、步骤执行器和设计器。
- [x] 固定 `LOCAL_TRANSACTION` / `ORCHESTRATION` 边界和兼容策略。
- [x] 固定发布态执行、结构化输入、可信上下文、条件更新和数值调整协议。
- [x] 建立 `spec.md`、`tasks.md`、`test-spec.md`、`execution-log.md`。

## 2. 后端执行底座

- [x] `/execute` 改为只解析最新发布动作，`/preview` 保留草稿预览。
- [x] 实现幂等键必填、动作发布版本入日志及版本化唯一域迁移。
- [x] 实现输入 Schema 投影、危险键拒绝和服务端可信系统上下文。
- [x] 实现执行模式/递归步骤白名单和本地主数据源事务约束。
- [x] 实现 `UPDATE_FIELD` 同 SQL 条件更新。
- [x] 实现 `ADJUST_NUMBER` 多字段原子增减、上下界和条件更新。
- [x] 日志记录执行模式和动作版本，不记录输入值。

## 3. 发布检查与设计器

- [x] 业务对象发布检查覆盖执行模式、输入 Schema、递归步骤和危险配置。
- [x] 自动化设计器增加执行模式、结构化输入和本地数据步骤配置。
- [x] 高级 JSON 继续兼容，但不新增 URL、SQL、认证或脚本入口。

## 4. 页面运行时

- [x] 手动 COMMAND 根据输入 Schema 生成表单并做类型/必填校验。
- [x] 每次动作确认生成并复用安全幂等键。
- [x] 请求只发送结构化输入和 recordId，不发送整行浏览器 context。

## 5. 验证与收尾

- [x] 后端服务、发布检查、动态更新和迁移契约测试通过。
- [x] 前端运行时/设计器定向测试和目标 ESLint 通过。
- [x] generator 相关模块聚合编译和前端生产构建通过。
- [x] `git diff --check`、安全静态检查、执行日志和长期决策回填。
- [x] Spec 状态改为 completed 并进入阶段 5。
