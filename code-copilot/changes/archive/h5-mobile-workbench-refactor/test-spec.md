# 增量验证计划

## P0

- `git diff --check` 检查本变更文件。
- 使用 Node `v20.19.0` 执行 `pnpm --dir forge-h5-ui build:h5`。
- 静态检查：待办首页计数调用待办接口；审批提交前校验策略，不存在动态/业务表单绕过提交路径。

## P1（用户环境回填）

- 登录后验证首页待办数、消息数与待办列表独立。
- 用候选任务验证签收、填写意见、同意/驳回。
- 用要求签名、动态表单、业务对象表单、代码表单各验证一次移动端失败关闭或完整处理行为。
- 验证流程分类/状态筛选、审批历史和回到列表后的刷新。

## 2026-07-28 增量验证

### P0

- 打开带 `id` 的消息页，确认详情只展示正文、已读状态和“查看业务”操作，不输出 `bizType`、`bizKey` 或 UUID。
- 确认首页仅展示待处理任务、消息提醒和菜单快捷入口；不展示菜单数、权限数或同步状态。
- 确认三项底栏的图标、文字和安全区不重叠，消息筛选标签可横向滑动且没有标签内数字拥挤。
- 运行 `git diff --check` 和 `pnpm --dir forge-h5-ui build:h5`。

## 2026-07-28 待办表单与动作增量验证

### P0

- 打开 `/pages/todo`，点击“全部流程”，验证分类弹层显示真实分类，选择、遮罩及右上角关闭均可关闭弹层并刷新列表。
- 打开采购申请等 `BUSINESS_CODE_FORM` 待办，验证 H5 先以 `taskId` 获取业务表单上下文并按节点权限展示字段；只读字段不可编辑、必填字段阻止提交。
- 修改可编辑业务字段后点击“暂存修改”，验证 `PUT /ai/business/flow/task-form-context` 成功并回显最新数据。
- 验证同意/驳回调用 `/ai/business/flow/task-action`；验证转办调用 `/api/flow/task/delegate`，退回/终结分别调用对应 Flow 接口。
- 运行 `git diff --check` 与 `pnpm --dir forge-h5-ui build:h5`。

## 2026-07-29 移动端流程闭环增量验证

### P0

- 节点设置 `requireSignature=true` 时，待办详情显示签名板；签名未完成不能提交，完成后应上传文件并把 fileId 作为 `signature` 传给 Flow/业务流程接口。
- 配置可写附件字段时，选择文件后应调用 `/api/file/upload`，保存/办理请求只提交服务端文件标识；无后端字段描述的可写组件仍不得提交。
- 转办弹层必须单独填写转办说明；按名称搜索、加载更多和选中人员均可工作，当前用户及当前办理人不应出现在候选中。
- 切换“已处理”“我发起的”分别调用 `/api/flow/task/done`、`/api/flow/task/started`；详情为只读，且能加载审批记录和流程节点进度；运行中的我发起流程可调用撤回。

### P1（用户环境回填）

- 用真实要求签名的节点、可写附件字段、已办记录和我发起流程各验收一次；确认 H5 与 PC 使用相同的节点权限、必填意见和后端结果。

## 2026-07-28 公共组件增量验证

### P0

- 确认 `AiSearchBar` 和 `AiField` 有边框、焦点态，且搜索框/表单输入高度分别为紧凑的 68rpx/80rpx。
- 确认首页快捷区不展示未注册的后端入口，优先处理和消息待办卡片的摘要保持单行紧凑展示。
- 确认个人页顶部不再展示固定联系方式；安全中心仅展示后端实际返回的手机号、邮箱和账号信息。
- 确认底栏选中态使用图标底板与边框区分，不依赖顶部短横。

## 2026-07-28 快捷入口与导航增量验证

### P0

- 确认首页第一个快捷入口为“组件演示”，可打开 `/pages/demo/loading/index`。
- 确认后端菜单（例如联系方式）同时出现在快捷区和全部应用内；专属页未编译时进入统一应用承接页，不触发“页面未注册”。
- 确认底栏恢复为浮动胶囊样式，并且优先处理区两行任务在小屏下保持紧凑。

## 2026-07-28 列表骨架与待办一致性验证

### P0

- 断网或节流进入 `/pages/todo`、`/pages/message/index`，确认首次列表查询显示骨架屏而非“加载中”文本或空白。
- 打开转办人员选择与租户切换，确认列表请求期间也显示紧凑骨架屏。
- 确认待办页使用公共搜索框、横向筛选标签和与消息页一致的白底边框列表卡片。
- 确认首页优先处理区只保留紧凑的两条行动项。
