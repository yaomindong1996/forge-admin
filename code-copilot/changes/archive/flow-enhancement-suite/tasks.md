# 实施任务

- [x] 增加模型多级退回字段、迁移和 DTO 返回策略。
- [x] 实现任务指定节点退回、驳回至发起人/修正后直送、用户侧改派 API，并补租户与流程实例安全校验。
- [x] 扩展待办业务展示 SPI 参数，保留业务类型、业务参数和扩展展示字段。
- [x] 在设计器支持发起人自选审批人及 `PROCESS_START_USER` 多实例 BPMN，并接入普通流程、低代码业务流程和应用级流程启动入口。
- [x] 允许用户自定义模型 Key，更新模型管理页。
- [x] 执行后端编译、前端相关单测、ESLint、生产构建和迁移静态规范检查。
- [ ] 由用户在配置数据库、Admin/Flow 服务后执行真实退回、改派、发起人自选审批人和业务待办 E2E 联调。


## Review-fix 增量任务（2026-08-26）

- [x] 修复发起人自选节点的 `flowable:assignee="${assignee}"` 绑定，并补转换器回归断言。
- [x] 约束并行分支下的指定退回/直送状态迁移，避免将多个活动分支合并到单节点。
- [x] 改派前校验用户启用状态、租户成员关系及租户有效期。
- [x] 保护普通启动和低代码启动的服务端业务上下文变量。
- [x] 将模型 Key 查询、通知模型加载统一收敛到显式租户与逻辑删除条件，并保留复制模型的多级退回开关。
- [x] 补充并运行模型、Mapper SQL、变量保护及任务状态迁移回归测试。
