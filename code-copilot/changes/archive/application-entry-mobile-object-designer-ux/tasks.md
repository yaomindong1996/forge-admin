# 实施任务

- [x] 确认 `visibleScope` 没有运行时读取或鉴权实现。
- [x] 确认 `mobileScene` 只用于移动入口统计，不改变打开目标。
- [x] 确认系统菜单和角色授权已按 `clientCode` 区分，移动 Web 客户端技术编码为 `h5`。
- [x] 精简入口向导和高级编辑器的移动配置，增加移动端菜单同步入口。
- [x] 让入口菜单同步服务按挂载端写入 `pc` 或 `h5` 客户端资源。
- [x] 更新 H5/移动端相关字典与客户端展示名称。
- [x] 将业务对象图标输入框替换为通用图标选择器。
- [x] 调整数据关系工作区与左侧导航间距。
- [x] 修复业务对象随应用发布时覆盖既有入口挂载端和菜单配置的问题。
- [x] 记录未执行自动化测试，由用户手工验收。

## 验证说明

- 按用户要求未执行自动化测试、构建或浏览器验证。
- 已完成源码引用检查和 `git diff --check`，最终功能由用户手工验收。
