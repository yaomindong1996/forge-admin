# 任务记录 - lowcode-permission-role-binding

## 已废弃方案

原 Task 1、Task 6、Task 7 使用 `node.access.roleIds` 保存页面角色并在发布时重建
`sys_role_resource`。该方案因运行态授权双真源被废弃；对象按钮资源自动注册能力继续保留。

## Task 1：应用范围授权协议与系统适配器

- [x] 新增应用权限工作台、角色授权状态和保存 DTO/VO。
- [x] 新增 generator 侧系统权限适配器接口与 admin 实现。
- [x] 为 app-server 和 flow-server 增加失败关闭的权限适配桥，保证运行态容器可装配且不开放管理操作。
- [x] 批量解析页面和对象按钮权限码对应的系统资源。
- [x] 返回当前操作者可维护角色，不绕过既有角色管理边界。
- [x] 增加聚合服务单元测试。

## Task 2：应用范围事务保存

- [x] 在系统角色服务新增通用的范围化授权保存方法。
- [x] 仅替换请求声明的资源范围，自动补齐必要父资源并防止权限溢出。
- [x] 仅替换请求声明的模块数据范围，保留其它模块覆盖。
- [x] 校验角色可维护性、数据范围上限和已绑定用户上限。
- [x] 提交后刷新数据范围缓存。
- [x] 增加系统角色服务单元测试或契约测试。

## Task 3：页面权限单一真源

- [x] 应用发布停止重建页面角色绑定，只维护资源目录。
- [x] 运行时按页面 RBAC 权限码过滤 `systemMenuVisible` 页面。
- [x] 非系统菜单内部页面继续随应用运行入口访问。
- [x] 移除页面设计器中的“访问控制”入口，保留存量 schema 兼容。
- [x] 更新发布与运行时单元测试。

## Task 4：低代码对象级数据范围

- [x] 动态数据权限使用 `ai:business:{objectCode}` 读取角色模块覆盖。
- [x] 空对象编码失败关闭到角色默认范围，不信任前端模块编码。
- [x] 增加 DynamicDataScopeService 单元测试。
- [x] 对象设计器“数据权限”改名为“数据范围适配”。

## Task 5：应用权限工作台

- [x] 复用系统角色管理 `RolePermissionSettings` 展示内核，不维护第二套权限卡片交互。
- [x] 顶部实现角色搜索选择、角色默认数据范围只读摘要和当前角色保存状态。
- [x] 将应用页面入口和绑定对象转换为应用范围权限模块。
- [x] 在同一权限工作台内实现页面入口、对象数据范围与按钮权限配置。
- [x] 应用模式下保持页面入口与按钮权限独立，不做隐式级联授权。
- [x] 对待发布资源、未启用 FOLLOW_SYSTEM 和加载失败提供明确状态。
- [x] 保存调用应用范围 PUT 接口，不调用全量角色资源接口。
- [x] 增加前端权限状态归一化和应用权限模块转换测试。

## Task 6：验证与记录

- [x] 运行 generator、system、admin 相关测试与编译。
- [x] 运行 app-center 前端测试、ESLint 和生产构建。
- [ ] 浏览器验证角色切换、页面勾选、对象数据范围和按钮勾选。
- [x] 把命令、结果、跳过项和服务清理情况写入 `execution-log.md`。

## Task 7：数据范围适配迁移到应用权限工作台

- [ ] 权限目录返回对象当前适配策略、共享影响范围和可选字段。
- [ ] 新增应用范围窄接口，只保存对象模型的数据范围策略字段。
- [ ] 应用权限对象卡片提供就地适配弹窗，不再跳转对象设计器。
- [ ] 适配保存后原地刷新当前对象和角色数据范围状态。
- [ ] 对象设计器移除数据范围入口，并保留独立的树形模型配置。
- [ ] 增加前后端增量测试并更新执行记录。
