# 验证范围

## 前端静态检查

- ESLint：仅检查本次修改的两个 Vue 文件及新增测试。
- Vite production build：确认 AiCrudPage schema、动态插槽与 Naive UI 组件可编译。

## 交互检查

- 列表：业务列可读、技术辅助信息不抢首屏、状态切换有确认且成功后刷新权威数据。
- 编辑：导出/导入/导入导出切换只显示相关设置；缺少数据源时阻止提交并给出业务化提示；技术设置默认折叠。
- 列配置：空态、添加/编辑、上移/下移边界、删除确认、保存 loading 与未保存提示。
- 视觉：亮色、暗色、390px 宽度，弹窗/抽屉不横向溢出。

## 边界

- 不连接真实数据库、Redis 或后端导出服务；API 请求使用预览桩验证页面行为。

## 本轮增量验证（2026-09-07）

- 修正复制方案 loading 的关闭方式后，重新执行相关文件 ESLint、状态开关组件 Vitest 和前端生产构建。
- 复跑既有 Playwright 预览，重点确认复制弹窗、亮暗色、390px 窄屏和文件列未保存提醒未回归。
- 新增断言：新建方案时“系统标识”输入框可编辑，编辑已有方案时保持锁定。
- 执行 `git diff --check`，并确认最终提交不包含工作区已有的 `.DS_Store` 变更。
