# Forge 流程管理模块补强 Tasks

> 变更名：`flow-management-hardening`  
> 关联 Spec：`spec.md`  
> 当前状态：实施中（部分 P0/P1 代码已完成；仍有未完成能力和真实环境验收阻断项）  
> 执行规则：每项任务按“测试 → 实现 → 目标验证 → 人工审查 → 记录”执行；不得跳过现有测试基线。

> 状态标记：`[x]` 已实现且有本地验证证据；`[~]` 部分实现或缺少集成证据；`[ ]` 尚未完成。

## 执行状态

- [~] Phase 0：基线、边界和访问矩阵（T0.1 已完成；T0.2 缺少完整矩阵集成验证）
- [~] Phase 1：任务/实例安全边界与动作状态机（任务终止/撤回和部分授权已修复；并发撤回、管理员专用权限和完整幂等验收仍待补齐）
- [~] Phase 2：超时与通知闭环（已有基础实现；扫描游标、通知重试和多租户调度验收仍待补齐）
- [~] Phase 3：监控性能与数据可信度（聚合统计和批量摘要已有实现；历史批量、压测和完整 SQL 计数证据仍待补齐）
- [~] Phase 4：审批能力补齐（管理员任务和候选用户集合变更已实现；临时抄送关系、完整父子加签和统一详情仍待补齐）
- [~] Phase 5：模型、节点和组织治理（结构/执行白名单和启动预览部分完成；表单引用、组织预检、资源与版本治理仍待补齐）
- [~] Phase 6：前端体验与接口契约（列表错误态和批量结果已实现；操作提交锁、统一 DTO/错误码和完整交互验收仍待补齐）
- [~] Phase 7：迁移、集成验证和灰度验收（本地自动化已补齐；真实依赖与灰度门禁仍待执行）

## Phase 0：基线、边界和访问矩阵（P0）

### [x] T0.1 建立变更基线

- **范围**：本变更目录、`code-copilot/rules/automated-testing-standard.md`、当前 Git 工作树。
- **步骤**：记录 `git status --short`、流程模块编译/测试基线、前端 lint/build 基线；列出 Flow/Admin/UI/DB 影响目录。
- **验收**：`spec.md` 中的非目标目录不会出现在后续代码变更清单；基线命令和结果写入 `execution-log.md`。

### [~] T0.2 定义流程访问矩阵

- **文件**：新增 `forge-server/forge-framework/forge-plugin-parent/forge-plugin-flow/src/main/java/com/mdframe/forge/starter/flow/security/FlowAccessGuard.java` 及测试。
- **步骤**：为任务详情、表单、流程图、历史、变量、评论、抄送、监控和管理动作定义“当前用户/发起人/处理人/候选人/管理员/租户”的允许关系。
- **测试**：覆盖跨租户、非参与人、候选人、发起人、处理人、监控查看者、监控管理员和清理管理员。
- **验收**：所有后续 Controller 和 Service 任务复用同一访问判定，不在 Controller 中复制条件。

## Phase 1：任务/实例安全边界与动作状态机（P0）

### [~] T1.1 收紧任务详情、表单、流程图和历史接口

- **文件**：`FlowTaskController.java`、`FlowTaskServiceImpl.java`、`FlowTaskMapper.xml`、`FlowBusinessMapper.xml`、新增访问异常 VO/错误码。
- **步骤**：移除详情接口对匿名权限的依赖；在 Service 入口调用 `FlowAccessGuard`；Mapper 查询增加租户和未删除条件；流程实例和业务关联查询使用同一 tenantId。
- **测试**：使用两个租户和三个用户验证 taskId/processInstanceId/businessKey 的读取矩阵；断言越权响应不泄露资源存在性。
- **验收**：任务、表单、历史、流程图、变量和业务数据全部通过租户/关系校验。

**实施记录（2026-09-05）**：新增 `FlowAccessGuard`，已接入任务详情、表单、流程图和历史入口；任务 Mapper 显式增加租户及未删除条件。变量/业务关联的完整覆盖仍需后续集成测试确认。

**Review 修复（2026-09-06）**：流程历史和任务表单在完成可见性校验后，统一复用租户限定的业务关联查询，移除同流程实例 ID 的无租户回查，避免详情响应再次落到其他租户业务记录。

### [~] T1.2 修复撤回 fail-open

- **文件**：`FlowTaskServiceImpl.java`、撤回相关测试。
- **步骤**：将缺少可信发起人信息的分支改为业务异常；为管理员修复操作单独定义权限和审计字段；确认撤回前校验流程状态和模型开关。
- **测试**：发起人匹配、非发起人、缺失业务发起人、缺失 initiator 变量、已完成实例、并发撤回。
- **验收**：无法确认发起人时撤回失败，且不改变 Flowable 或业务表状态。

**实施记录（2026-09-05）**：缺失业务发起人和 `initiator` 时改为拒绝并记录 warning；并发撤回和管理员专用权限尚未补齐。

### [~] T1.3 补齐催办、委派、转派、终止和删除授权

- **文件**：`FlowTaskController.java`、`FlowTaskServiceImpl.java`、`FlowMonitorController.java`、对应 DTO/权限资源 SQL。
- **步骤**：为催办增加明确权限；Service 层校验租户、任务状态、操作者关系和目标用户；删除流程数据增加二次权限、原因和范围校验。
- **测试**：无权限、跨租户、任务未签收、已完成任务、非候选人、管理员和合法处理人场景。
- **验收**：所有动作在绕过 UI 直接调用时仍被正确拒绝或允许。

**实施记录（2026-09-05）**：委派和任务终结增加租户/业务归属及操作者关系校验；管理员专用权限与目标用户组织校验仍待补齐。

### [~] T1.4 建立任务动作幂等和状态转换合同

- **文件**：`FlowTaskServiceImpl.java`、`FlowInstanceServiceImpl.java`、幂等 Mapper/迁移、动作测试。
- **步骤**：统一读取 idempotencyKey/requestDigest；在数据库唯一约束或分布式锁下执行动作；定义 TODO、CLAIMED、APPROVED、REJECTED、WITHDRAWN、TERMINATED 等状态转换矩阵。
- **测试**：重复请求、并发请求、网络超时重试、状态已变更、幂等键复用不同 payload。
- **验收**：同一动作最多推进一次，冲突返回稳定错误码，Flowable、业务表和通知事件状态一致。

**Review 修复（2026-09-06）**：签收、审批、驳回、改派、加签/减签、委派和终结等任务动作统一使用租户限定的 `SELECT ... FOR UPDATE`；保留无租户锁方法仅为历史兼容且业务代码不再调用。幂等键冲突和真实并发事务仍需 MySQL/Redis 集成验证。

## Phase 2：超时与通知闭环（P0/P1）

### [~] T2.1 持久化任务截止时间和超时配置

- **文件**：`FlowNodeConfig` 实体/Mapper、Flyway migration、`FlowNodeConfigServiceImpl.java`、部署校验服务。
- **步骤**：明确节点超时单位、时区、工作日规则和升级策略；保存可计算字段或转换为 Flowable due date；为旧配置提供默认值。
- **测试**：分钟/小时/天、时区、空配置、非法配置、重新部署和版本回退。
- **验收**：运行中任务可查询准确 due date，配置变更不影响既有实例的历史口径。

### [~] T2.2 启用批量超时扫描

- **文件**：`FlowTimeoutServiceImpl.java`、调度配置、扫描游标/指标、测试。
- **步骤**：启用调度；按 due date、租户和稳定游标读取任务；批量加载节点配置和实例；记录扫描批次、耗时、成功/失败数量。
- **测试**：无任务、单批、多批、并发扫描、任务完成后扫描、扫描异常重试和跨租户。
- **验收**：扫描不会全表 offset 漫游，重复扫描不会重复产生超时事件。

**实施记录（2026-09-05）**：已启用定时扫描并按批次读取活动任务；稳定游标、租户级进度和事件幂等仍待实现。

**Review 修复（2026-09-06）**：任务创建事件根据节点超时配置写入 Flowable 原生 `dueDate`；超时扫描改为按 `taskDueBefore(now)` 和截止时间排序分页，临近超时查询改为 dueDate 时间窗口并限制最大提前分钟数，减少每轮遍历全部活动任务的开销。旧实例、稳定游标、租户级进度和通知事件幂等仍需集成环境补齐。

**租户修复（2026-09-06）**：节点配置运行时查询改为 Mapper XML 显式绑定流程模型和节点配置的 `tenant_id`，并拒绝缺少可信租户上下文的查询；逾期提醒解析同步传递租户 ID。

**时区修复（2026-09-06）**：任务截止时间回填、Flowable `Date` 与 Forge `LocalDateTime` 的转换统一使用 `forge.flow.timeout.time-zone`，默认 `Asia/Shanghai`，非法配置自动回退并告警，避免依赖服务器系统时区造成跨环境偏移。

**扫描游标修复（2026-09-06）**：超时扫描改用 `sys_flow_task` 本地快照按 `due_date + id` 稳定游标分页，再在任务所属租户上下文内查询和处理 Flowable 活动任务；不再使用 Flowable 全局 offset 查询，避免多租户调度时跨租户配置回查和活动结果集变化导致漏扫。新增 `V1.0.145` 复合索引支持游标查询；每轮限量回填旧任务缺失的 `due_date`，按 `create_time + id` 游标计算并同步 Flowable 截止时间。稳定游标目前为单轮调度内游标，跨实例租户级进度仍需 Redis/数据库集成验收。

**继续开发（2026-09-06）**：超时扫描增加 Redis 分布式互斥锁（Redis 不可用时降级为单 JVM 锁），锁租期覆盖单轮扫描并使用 token 校验释放，避免多实例同时处理同一批任务产生重复超时动作；原有 `due_date + id` 稳定游标仍作为批内分页依据。

### [~] T2.3 实现超时通知、升级和停止提醒

- **文件**：`FlowOverdueReminderServiceImpl.java`、消息发送适配器、提醒记录 Mapper、字典/模板 SQL。
- **步骤**：实现站内信基础通知和可选渠道；使用提醒记录幂等；支持首次、重复、升级和已完成停止；记录失败原因和下一次重试时间。
- **测试**：渠道未配置、发送失败、重复扫描、候选人任务、多人任务、任务完成和重试退避。
- **验收**：通知记录、消息状态和任务超时状态可追踪，接口不返回伪成功。

**实施记录（2026-09-06）**：已接入 `MessageService.sendIfAbsent`，按租户、提醒批次和渠道幂等落库；发送失败记录原因并按指数退避重试。补充发送前任务状态门禁，任务完成、撤回或终结后停止后续提醒。升级通知、工作日/时区规则和真实 EMAIL/SMS 适配仍待集成环境确认。

### [~] T2.4 修复监控超时统计和筛选

- **文件**：`FlowBusinessMapper.xml`、`FlowMonitorServiceImpl.java`、`FlowMonitorController.java`、`forge-admin-ui/src/views/flow/monitor.vue`、`api/flow.js`。
- **步骤**：增加 overdue 查询参数或独立任务接口；统计使用任务 due date；移除固定 0；前端卡片点击传递正确参数；能力不可用时显示不可用状态。
- **测试**：逾期/未逾期/已完成/挂起/不同租户和分页筛选。
- **验收**：点击超时卡片能得到真实超时任务，不再以流程实例状态 `timeout` 查询。

**实施记录（2026-09-05）**：统计改用 `due_date` 与活动任务状态，实例查询支持 `overdue` 参数，前端改用 `pageNum` 和 `searchForm.overdue`。

## Phase 3：监控性能与数据可信度（P1）

### [~] T3.1 消除监控实例列表 N+1

- **文件**：`FlowMonitorServiceImpl.java`、`FlowBusinessMapper.xml`、监控 VO。
- **步骤**：先批量取得当前 processInstanceId 集合，再批量获取任务和用户名称；Mapper 一次返回分页记录和当前节点摘要。
- **测试**：10/100/1000 条分页记录，断言 SQL 次数不随记录数线性增长；验证缺少任务和用户时的默认值。
- **验收**：监控分页响应时间和 SQL 数量达到基线目标。

**实施记录（2026-09-05）**：监控实例页改为按当前页实例 ID 一次批量读取活动任务摘要，消除逐实例 Flowable 任务查询；用户名称解析仍依赖现有 SPI，聚合统计和压测待后续完成。

### [x] T3.2 将平均耗时和趋势改为聚合查询

- **文件**：`FlowMonitorServiceImpl.java`、监控 Mapper XML、统计 VO。
- **步骤**：使用 `AVG`、`COUNT`、按日期聚合和索引过滤替代全量 `list()`；明确完成时间为空的统计口径。
- **测试**：空数据、单条、百万级模拟数据、跨天边界、时区和租户隔离。
- **验收**：JVM 不持有全量历史实例对象，统计口径和旧结果对照可解释。

**实施记录（2026-09-05）**：流程实例统计新增 `selectProcessInstanceStats` 聚合 SQL，按租户和流程定义聚合运行中数量、已结束数量和平均耗时；服务层不再通过 Flowable 历史接口全量加载实例。趋势查询原已有按日期聚合 SQL，跨租户约束保持不变。

### [~] T3.3 历史、流程图和用户信息批量化

- **文件**：`FlowTaskServiceImpl.java`、历史/用户查询 Mapper、流程图 VO。
- **步骤**：增加分页和最大返回量；批量查询审批点、用户、组织和候选人；补齐已执行连线计算。
- **测试**：长流程、多次循环节点、并行会签、候选人任务、无历史实例和超限分页。
- **验收**：详情接口返回完整且响应大小可控，流程图节点和连线状态一致。

**实施记录（2026-09-06）**：流程历史、流程图节点和审批任务读取增加 `MAX_DETAIL_HISTORY_ITEMS=1000` 上限，避免超长流程一次性撑爆内存；新增契约测试覆盖历史集合上限。用户/组织批量解析与已执行连线完整计算仍待补齐。

**性能修复（2026-09-06）**：新增租户限定的 `selectFlowUserInfoBatch`，流程图节点处理人/候选人详情通过批量接口并在同一详情请求内缓存，移除逐用户组织回查；批量查询保留启用用户、有效租户成员、组织/岗位状态及 200 条上限。

**分页协议补齐（2026-09-06）**：新增类型化 `FlowHistoryPageVO`/`FlowHistoryItemVO` 和 `/api/flow/task/history/{processInstanceId}/page`，历史任务由 XML 按租户、创建时间和主键分页，单页上限 1000，返回 `total/hasMore`；旧 List<Map> 接口保留兼容，避免现有页面立即改造。

### [~] T3.4 修复监控错误边界

- **文件**：`FlowMonitorServiceImpl.java`、`FlowMonitorController.java`、`monitor.vue`。
- **步骤**：将查询异常映射为稳定错误码；需要降级时返回 `degraded=true` 和原因；前端显示错误状态和重试按钮。
- **测试**：数据库异常、Flowable 异常、用户服务异常、单个统计失败和全部统计失败。
- **验收**：后端故障不会显示成“暂无数据”或 0。

**实施记录（2026-09-05）**：统计接口异常返回 `degraded=true` 和稳定错误码，前端显示告警及重试；实例列表异常显示错误态，避免与空数据混淆。

## Phase 4：审批能力补齐（P1）

### [~] T4.1 管理员任务和实例聚合详情

- **文件**：新增任务/实例 Controller、DTO/VO、Service、Mapper；`forge-admin-ui/src/api/flow.js` 和管理页面。
- **步骤**：提供管理员任务分页、按实例查询全部任务、审批详情聚合和任务树；复用 `FlowAccessGuard` 和监控权限。
- **测试**：管理员/普通用户、跨租户、已完成/运行中实例、分页和任务树关系。
- **验收**：管理员可以在授权范围内定位任务并看到完整审批上下文。

**实施记录（2026-09-05）**：新增 `/api/flow/monitor/instance/{processInstanceId}/tasks` 分页接口，按租户和逻辑删除过滤查询实例下全部任务；监控详情抽屉已接入分页任务上下文表格和错误重试；动态任务树和更深层详情聚合仍待补齐。

**实施记录（2026-09-06）**：实例详情新增按创建时间稳定排序、最多 500 条的管理员任务树；节点按 `taskDefKey` 聚合，子节点展示办理人、状态、活动标记和完成时间，并返回 `currentTaskIds` 与 `taskTreeTruncated`。新增 `FlowMonitorTaskTreeContractTest` 覆盖租户条件、LIMIT 上限和活动任务筛选。真实管理员权限、并行/会签关系和浏览器验收仍待补齐。

### [~] T4.2 可退回节点、取消语义和下一节点预览

- **文件**：`FlowTaskServiceImpl.java`、`FlowInstanceServiceImpl.java`、BPMN 解析服务、对应 Controller/VO、todo/started 页面。
- **步骤**：增加可退回节点查询；区分发起人取消、管理员取消、终止和撤回；实现下一节点与候选审批人预览；解析失败阻断发起。
- **测试**：线性、网关、会签、发起人修改回路、无候选人、已完成和挂起流程。
- **验收**：用户执行动作前能看到目标节点和影响，状态转换可审计。

**Review 修复（2026-09-06）**：管理员回退在 Flowable 状态变更前校验当前实例仍运行、存在活动节点、目标 ID 确实属于当前流程定义且不是当前活动节点；不再把任意字符串直接交给 `changeActivityState`。

**转派边界修复（2026-09-06）**：管理员转派在锁定当前租户流程后，使用 `selectByTaskIdAndTenant` 回查本地任务快照，禁止无租户条件的任务更新。

**委派状态修复（2026-09-06）**：普通委派和发起人改派在 Flowable `setAssignee` 前校验目标用户属于当前租户且处于启用状态；任务镜像状态统一写为 `CLAIMED`，避免已直接指定办理人的任务继续显示为待办。

### [~] T4.3 动态加签、减签和临时抄送

- **文件**：新增加签/抄送实体、Mapper、Flyway、Service、Controller、前端任务详情和抄送页面。
- **步骤**：建立原任务、父任务、加签任务和临时抄送关系；支持前加签、后加签、减签、撤回抄送和已读；所有动作记录原因和操作者。
- **测试**：串行、并行会签、重复加签、撤回、任务完成后操作、跨租户和权限矩阵。
- **验收**：动态任务关系在流程图、历史、待办和管理员页面可追踪。

**实施记录（2026-09-05）**：先完成现有抄送查询和读状态变更的安全收敛：我的抄送、已发送抄送、未读统计、单条/批量已读均显式限制当前租户；已读操作仅允许抄送接收人；表单详情通过 `getVisibleById` 限制为接收人或发送人。新增 `/api/flow/task/add-sign` 与 `/api/flow/task/reduce-sign`，在当前任务候选办理人集合上执行安全加签/减签，并写入 Flowable 评论审计；临时抄送关系模型仍待后续实现。

**实施记录（2026-09-06）**：动态加签拒绝将当前办理人或操作者再次加入候选集合，并限制单任务最多 50 名动态加签人，降低重复加签和候选集合膨胀风险；临时抄送已增加租户内 ACTIVE/REVOKED 关系状态、发送人撤回接口、撤回审计字段和接收方可见性收敛。父子加签关系仍待后续实现。

**继续开发（2026-09-06）**：候选关系表增加 `parent_task_id`、`sign_mode`、`operator_id`、`reason` 审计字段，动态加签/减签写入父任务和操作者关系；新增租户限定的 `GET /api/flow/task/{taskId}/sign-relations` 查询接口，结果最多 50 条并保留 ACTIVE/REVOKED 状态。`BEFORE`/`AFTER`/`PARALLEL` 模式已纳入关系协议，当前 Flowable 执行仍采用候选任务并行语义，前后置专用编排待多实例集成阶段补齐。

**继续开发（2026-09-06）**：对 BPMN 已声明多实例的用户任务接入 Flowable 原生 `addMultiInstanceExecution/deleteMultiInstanceExecution`，保存动态关系对应的 `child_task_id/child_execution_id`，减签按子执行精确删除；普通用户任务继续走候选关系兼容路径。新增 `V1.0.154` 运行关系字段迁移和 `V1.0.155` 加签模式/关系状态字典；发起列表展示动态关系、模式和撤回状态，动态加减签请求统一带幂等凭证。真实多实例 Flowable、顺序/前后置语义和并发减签仍需集成验证。

**Review 修复（2026-09-06）**：由于当前 Flowable 动态执行实现只覆盖并行关系，`BEFORE`/`AFTER` 不再降级伪装为并行加签；服务端统一返回 `FLOW_TASK_SIGN_MODE_UNSUPPORTED`，发起页禁用不可执行选项并显示能力提示，保留协议/字典字段供后续顺序编排实现。

## Phase 5：模型、节点和组织治理（P1/P2）

### [~] T5.1 部署前 BPMN 和表单完整性校验

- **文件**：`FlowModelServiceImpl.java`、`FlowBpmnGenerateService.java`、设计器 API、部署测试。
- **步骤**：校验开始/结束节点、连线、网关条件、表单引用、节点权限、监听器、超时配置和模型版本；输出节点级诊断。
- **测试**：缺开始/结束、悬空连线、空条件、缺表单、无审批人、非法监听器和未支持执行节点。
- **验收**：不完整模型不能发布，错误信息包含节点名称和修复建议。

**实施记录（2026-09-05）**：部署前增加开始节点、结束节点、连线引用和悬空连线校验；增加执行节点预检：拒绝 scriptTask/callActivity/subProcess，serviceTask 仅允许声明 `flowable:type=cc`，userTask 必须配置处理人/候选用户/候选组，互斥/包容网关分支必须有条件或默认分支。表单引用和运行时组织成员的深度解析仍待后续补齐。

### [~] T5.2 执行节点白名单和 raw XML 风险收敛

- **文件**：`AdvancedConfig.vue`、节点类型常量、Flowable 部署校验和执行委托注册。
- **步骤**：列出已支持节点；未支持的 service/script/subProcess/callActivity 禁止部署或明确标记人工扩展；设计器显示执行限制。
- **测试**：每种支持/不支持节点的部署和运行；raw XML 修改后重新校验。
- **验收**：不存在“设计器可保存但运行无效果”的未声明节点。

### [~] T5.3 组织解析预检、用户组和表达式资源

- **文件**：`FlowNodeConfigServiceImpl.java`、`FlowOrgIntegrationServiceImpl.java`、新增用户组/监听器/表达式 Controller、实体、Mapper、前端页面和 SQL。
- **步骤**：将审批人解析从运行时静默空列表改为预检报告；增加用户组 CRUD、流程监听器和表达式资源管理；普通用户路径不暴露 SpEL 细节。
- **测试**：用户/角色/部门/用户组解析、禁用用户、空组、表达式语法错误、租户隔离和权限。
- **验收**：发起前可发现审批人缺失，资源引用关系可查询和审计。

**实施记录（2026-09-06）**：启动配置接口新增 `preflightPassed` 和 `diagnostics`，对首批可达用户任务静态检查 `assignee`、`candidateUsers`、`candidateGroups`；发起人自选节点不误报。模型测试弹窗展示节点级缺失审批人诊断。组织用户详情和选人列表改为显式租户 SQL，过滤禁用/删除用户、无效租户成员、禁用组织和禁用岗位；角色候选解析支持跨当前活动组织读取有效成员，选人列表限制最多 200 条并避免逐用户组织回查。表达式模板管理已补充租户显式查询、分页上限、启用列表上限、逻辑删除、租户内编码唯一校验和 DTO 写接口。用户组 CRUD、成员维护、菜单/API 权限和运行时 `candidateGroups` 解析已补齐；普通用户界面仍通过模板选择使用表达式原文，运行时表达式深度解析和高级能力分层仍待补齐。

**Review 修复（2026-09-06）**：运行时 `FlowSpelService` 的角色、部门角色和区域用户解析统一改走租户感知的 `FlowOrgIntegrationService`；新增区域用户 SQL，过滤无效租户成员和禁用用户，动态结果限制 200 条，并移除流程变量整包和审批人 ID 的 info 日志。真实 Flowable 表达式执行仍待集成环境验证。

**继续开发（2026-09-06）**：组织层级递归解析改为 `SysOrgMapper.selectOrgAndChildrenIdsByTenant` 和带租户的直接子组织查询，显式过滤启用组织和逻辑删除数据，移除流程组织递归路径中的 Service Lambda 查询。启动配置预检新增静态 `candidateGroups` 解析：角色编码/角色 ID/部门 ID/自定义用户组均检查当前租户内启用成员，动态 SpEL 候选组保留到运行时求值；无成员时返回节点和候选组诊断并将 `preflightPassed=false`。

**继续开发（2026-09-06）**：节点配置和条件规则统一使用 `FlowSafeExpressionEvaluator`，以只读数据绑定上下文替代 `StandardEvaluationContext`，禁止类型引用、Bean 引用、构造器和反射链，限制表达式长度 2000，并兼容 `${...}`/`#{...}` 包裹；运行失败只返回安全告警，不回显表达式内容。补充正向、恶意表达式和源码契约测试。

### [~] T5.4 模型治理能力

- **文件**：模型 Controller/Service/Mapper、版本页面、Flyway（如需字段）。
- **步骤**：增加流程定义分页/简单列表、模型批量排序、历史版本清理和清理前引用检查；保留版本审计和回滚信息。
- **测试**：多版本、运行中引用、清理权限、排序并发、回滚和租户隔离。
- **验收**：模型生命周期和版本数据可维护，不影响运行中实例。

**实施记录（2026-09-06）**：模型列表、Flowable 版本查询和版本列表均限制最多返回 100 条；版本详情、比较、回退、标签和删除按当前租户读取；版本删除改为逻辑删除，且存在运行中实例时拒绝清理。版本引用检查、批量排序和完整回滚演练仍待补齐。

**Review 修复（2026-09-06）**：版本回退改用租户限定模型查询，新增版本记录沿用当前租户，不再写死租户 1；版本标签使用租户限定的条件更新；Flowable 运行时服务未初始化时拒绝删除带流程定义引用的版本，避免引用检查 fail-open。`FlowModelVersionGovernanceContractTest` 增加上述约束。

**继续开发（2026-09-06）**：新增 `POST /api/flow/model/version/cleanup` 历史版本清理接口，使用明确 `VersionCleanupDTO` 和 `VersionCleanupVO`；候选版本按租户 `SELECT ... FOR UPDATE` 稳定排序，默认保留最近 10 个（可配置 1-100），发布/废弃版本、当前模型版本和仍被运行中实例引用的版本均跳过，其余通过租户限定逻辑删除并返回扫描、删除和跳过分类结果。

**继续开发（2026-09-06）**：补齐模型目录批量排序。新增 `sort_order` 字段及租户/逻辑删除/排序索引，迁移时按原创建时间倒序回填；新增 `FlowModelSortDTO`、租户锁定的 `selectByIdsForUpdate`、批量排序接口 `POST /api/flow/model/sort` 和独立 `flow:model:sort` 权限。前端模型卡片支持拖动排序、保存锁和失败反馈，排序保存仅提交当前页模型并按稳定页序分配值；重复模型、跨租户 ID、非法排序值和并发更新均拒绝。

**缺陷修复（2026-09-06）**：模型排序锁查询经过租户 SQL 解析器后出现 `FOR UPDATE ORDER BY`，导致 MySQL 语法错误；已从 `selectByIdsForUpdate` 移除无业务意义的排序子句，保留租户锁和逻辑删除边界，契约测试增加锁查询不能在 `FOR UPDATE` 后出现 `ORDER BY` 的断言。

**继续开发（2026-09-06）**：补齐任务转办、动态加签和减签的请求幂等凭证。公共任务动作 DTO 新增 `idempotencyKey/requestDigest`；转办成功后将 `DELEGATE` 凭证写入租户限定任务镜像，原操作者通过任务 owner 重试时返回幂等成功，复用不同载荷返回冲突。候选关系表新增幂等键和请求摘要字段及索引；同一任务关系锁内重复加签/减签只执行一次，减签保留审计状态，缺少关系持久化能力时拒绝带凭证请求。

## Phase 6：前端体验与接口契约（P1）

### [~] T6.1 批量审批逐条结果和重复提交保护

- **文件**：`forge-admin-ui/src/views/flow/todo.vue`、`src/api/flow.js`、批量结果 VO。
- **步骤**：批量操作前预检需要表单/签名的任务；提交后展示成功、失败、跳过列表；所有动作使用统一 loading 锁并在失败后保留可重试项。
- **测试**：全成功、部分失败、全部跳过、重复点击、任务已被他人处理和网络超时。
- **验收**：用户不会误以为跳过任务已审批，结果可追踪。

**实施记录（2026-09-05）**：批量快捷审批保留逐条成功/失败结果，失败任务留在弹窗中并支持“重试未处理任务”；需表单或签名的任务仍按策略跳过并展示原因。发起流程详情增加动态加签/减签入口，统一调用服务端安全接口。

**继续开发（2026-09-06）**：抽取共享流程动作幂等凭证生成器，发起流程详情的加签/减签增加 UUID、SHA-256 摘要、模式选择和关系状态列表，和待办动作使用同一协议。

### [~] T6.2 列表数量、错误态和加载态

- **文件**：`todo.vue`、`done.vue`、`started.vue`、`cc.vue`、`monitor.vue`、对应统计 API。
- **步骤**：使用服务端总数；区分 loading/empty/error；失败提供重试；详情中的表单、历史、流程图分别显示状态。
- **测试**：空数据、接口失败、慢响应、分页切换、统计接口部分失败。
- **验收**：数量不受当前页影响，错误不会伪装为空列表。

**实施记录（2026-09-05）**：待办、已办、我发起、抄送和监控列表增加区域错误提示与重试入口；抄送后端查询和已读操作增加租户/参与人边界；抄送未读徽标改用服务端总数，避免只统计当前分页。

### [~] T6.3 模型和监控操作提交锁、确认和反馈

- **文件**：`model.vue`、`form.vue`、`design.vue`、`monitor.vue`。
- **步骤**：发布、复制、挂起、激活、删除、清理、终止、转派和回退操作增加请求锁、状态刷新、权限提示和错误 requestId。
- **测试**：连续点击、浏览器返回、接口超时、权限变化和操作成功后列表刷新。
- **验收**：不会产生重复请求；成功、失败和部分成功均有明确反馈。

**实施记录（2026-09-06）**：模型页已为发布、复制、挂起、激活、删除和发起测试增加按模型/动作维度的操作锁，确认取消、请求完成和异常路径均释放锁；监控页已为清理、删除、挂起、激活、终止、回退、转派、错误重试和标记解决增加互斥提交状态，成功后刷新列表、统计和当前实例上下文，失败提示携带可用 requestId。`form.vue`、`design.vue` 的同类操作和真实浏览器连续点击验收仍待补齐，因此保持部分完成。

**实施记录（2026-09-06）**：表单设计器保存、发布、复制、删除、启停增加按记录和动作维度提交锁；流程设计器保存与部署互斥，部署内部保存使用受控锁。真实浏览器连续点击验证仍待集成环境补跑。

### [~] T6.4 DTO/VO 和错误码收敛

- **文件**：Flow Controller、`src/api/flow.js`、新增前端类型/响应适配。
- **步骤**：将监控、历史、审批详情和管理动作的固定字段改为明确 DTO/VO；保留 variables 为动态 Map；统一错误码和消息字段。
- **测试**：契约测试覆盖分页、动作、错误响应和兼容字段。
- **验收**：前后端字段可发现，禁止新增整包 `Map<String,Object>` 写接口。

**实施记录（2026-09-06）**：审批历史新增明确 VO 分页读接口和前端 API 封装；旧历史接口仅作为兼容适配，不新增 Map 写请求。监控、表单和管理动作中其他固定 Map 响应仍待继续收敛。

**继续开发（2026-09-06）**：监控实例列表、实例详情和实例任务树改为 `FlowMonitorProcessInstancePageVO`、`FlowMonitorProcessInstanceDetailVO`、`FlowMonitorTaskPageVO` 及任务树子 VO；保留 `list/total/pageNum/pageSize/taskTree/currentTaskIds/taskTreeTruncated` JSON 字段。实例列表查询异常返回 `degraded=true` 与稳定 `FLOW_MONITOR_INSTANCES_UNAVAILABLE`，前端监控列表据此进入错误态而不会显示成空列表。

**继续开发（2026-09-06）**：错误日志、流程表单和表达式模板分页改为 `FlowErrorLogPageVO`、`FlowFormPageVO`、`FlowSpelTemplatePageVO`；错误日志异常返回 `degraded=true` 与 `FLOW_ERROR_LOGS_UNAVAILABLE`，并统一限制分页大小 100，保留旧 `list/records/page/pageNum/pageSize/total` 字段兼容。

**继续开发（2026-09-06）**：前端抽取 `flow-action-idempotency.js`，统一待办和发起详情的动作摘要生成，避免各页面实现漂移；发起详情加签/减签增加模式、关系审计展示和重复提交凭证。

**继续开发（2026-09-06）**：监控历史活动、当前任务、模型状态统计、Flowable 模型版本摘要和抄送未读数量改为明确 VO，保持既有 JSON 字段兼容；新增控制器边界契约测试，禁止这些固定字段响应回退为 `Map`。

**继续开发（2026-09-06）**：抄送列表的标题搜索参数此前只由前端发送、服务端未参与查询，导致“流程名称或编号”搜索实际无效；新增租户限定的 XML 分页查询，覆盖标题、摘要、流程定义 Key 和业务 Key，补充稳定的 `cc_time + id` 排序并限制搜索词长度。未读统计改用带租户和有效状态条件的 Mapper 聚合，撤回抄送不会继续占用未读数量。

**继续开发（2026-09-06）**：修正抄送页“全部已读”只提交当前分页记录的问题，新增 `/api/flow/cc/read/all` 租户限定批量更新接口；撤回关系不再允许接收人继续标记已读，单条标记在资源不可见时返回稳定错误。前端展示实际更新条数并在成功后刷新列表和未读徽标。

## Phase 7：迁移、集成验证和灰度验收（P0/P1）

### [~] T7.1 候选关系表和逻辑删除迁移

- **文件**：`forge-server/db/migration/`、候选关系实体/Mapper、清理 Service、数据迁移脚本。
- **步骤**：创建用户/角色/部门/用户组关系表；回填现有逗号字段；双读校验；切换新表；评估流程业务数据逻辑删除和历史清理策略。
- **测试**：重复回填、脏数据、跨租户、删除后重建、迁移中断和重跑。
- **验收**：候选查询使用索引；迁移前后任务集合一致；脚本可重复执行并有回滚说明。

**实施记录（2026-09-06）**：新增 `sys_flow_task_candidate` 候选关系表、实体、枚举、Mapper 和 Flyway `V1.0.147`；迁移脚本按租户从逗号字段幂等回填用户/组关系，候选查询优先使用带索引的关系表并保留旧字段回退；任务创建、动态加签和减签已同步关系表。任务动作锁查询进一步改为 `task_id + tenant_id FOR UPDATE`，避免 `@IgnoreTenant` 边界下先锁跨租户行再在 Java 层拒绝。真实 MySQL/Flyway 重跑、中断恢复、执行计划、逻辑删除历史清理和迁移回滚尚未验证。

**继续开发（2026-09-06）**：新增 Flyway `V1.0.148__add_flow_task_sign_relation_audit.sql`，以 `information_schema` 防重复增加动态加签父任务、模式、操作者、原因字段及父任务索引；关系写入支持减签审计和重新加签时更新审计信息。XML、迁移占位符和静态契约已验证，真实 Flyway 中断重跑仍待 T7.2/T7.3。

### [~] T7.2 自动化测试和性能验证

- **范围**：Flow 插件和 Flow 服务测试、前端 Vitest、必要的 Testcontainers。
- **步骤**：先读取 `code-copilot/rules/automated-testing-standard.md`；补充安全契约、动作状态机、超时、动态加签、监控聚合、前端批量结果测试；执行 Maven compile/test、前端 lint/build 和性能基线。
- **验收**：测试命令、结果、警告、跳过项和服务清理记录在 `execution-log.md`；失败项有阻断说明。

**构建验证修复（2026-09-06）**：根 POM 的 Surefire 分组参数会在无测试引擎的依赖模块初始化时失败；父 POM 现在统一提供 BOM 管理的 `junit-jupiter-engine` 测试依赖，保留 `dev`/`exclude` 标签过滤并使无测试模块安全跳过。流程用户组契约测试和无测试引擎模块回归已通过；Mockito/Byte Buddy attach、真实数据库/Redis/Flowable 和性能压测仍属于集成门禁。

**打包行为（2026-09-07）**：默认 `forge.tests.skip=true`，发布打包不执行测试用例；`-Penable-tests` 显式开启测试并保留契约测试入口。默认流程插件 Reactor `package` 已验证通过。

**追加修复（2026-09-07）**：发现编译器插件的通用 `<skip>` 配置覆盖了 Maven 的 `maven.test.skip`，导致默认 `package` 仍编译测试源码。已改为主编译使用 `skipMain`、测试编译使用 `skip`，并完成全工程 `mvn package` 验证（51/51 模块成功）。

### [ ] T7.3 灰度、人工审查和回滚演练

- **步骤**：内部环境 → 单租户 → 低风险租户 → 全量；每阶段记录越权测试、超时通知量、接口延迟、错误率和消息积压；演练配置回滚、调度停止、迁移重跑、动态加签补偿和数据库恢复。
- **人工审查**：安全负责人、流程产品负责人、运维负责人和数据负责人分别签收对应门禁。
- **验收**：所有 P0/P1 验收标准通过，未通过项形成新的变更或明确阻断，不得以隐藏错误替代修复。

## 任务完成定义

- `spec.md` 中所有 P0/P1 需求均有对应任务、测试和验收证据。
- 代码、Mapper、迁移、前端、文档和权限资源未超出非目标范围。
- `test-spec.md`、`execution-log.md` 已记录增量测试、警告、跳过项、服务清理和人工审查结果。
- 生产灰度前完成访问控制、超时通知、数据迁移、备份/回滚和动态加签的人工签收。

### [~] T6.5 人工抄送发送授权

- **状态**：部分完成，真实流程启动阶段和无会话回调仍需集成验证
- **实现**：人工发送入口强制绑定当前会话用户；服务层校验租户下流程实例、业务Key/流程定义、来源任务参与关系、发起人/历史参与人关系和目标用户可用性，并限制单次接收人数量。Flowable 回调和系统通知保留独立内部发送入口。
- **验证**：`FlowCcSendSecurityContractTest` 通过；Java 17 Flow/Flow Server 依赖编译通过。

### [~] T5.5 发起前下一节点预览

- **状态**：部分完成（只读预览，未做运行时条件求值）
- **实现**：扩展 `/api/flow/model/key/{modelKey}/start-config` 响应的 `nextNodes`，从开始事件沿 BPMN 可达路径解析首批用户任务，返回节点名称、是否多实例及 assignee/candidateUsers/candidateGroups 原始策略；不执行表达式、不泄露流程变量。
- **验证**：`FlowStartPreviewContractTest` 通过；Flow/Flow Server Java 17 编译通过。
- **边界**：运行时组织解析、网关条件分支和真实候选人可用性仍以发布前模型校验与启动时 Flowable 解析为准，当前接口是只读预览。

## 本轮 Review 修复（2026-09-06）

- 修复 `sys_flow_task` 运行时表不存在 `del_flag/update_time` 导致的 Mapper 运行时 SQL 错误；相关查询恢复为现有表结构。
- 终止和撤回改为删除 Flowable 实例前捕获活动 taskId，删除后只修正这些活动任务，避免覆盖历史已通过、已驳回或已取消任务。
- 修正任务状态 5 的历史动作文案为“cancel”，新增状态字典 7/8 并对齐“已取消”文案，发起列表不再硬编码排除新状态。
- 抄送内部回调租户解析支持 `TenantContextHolder`，启动阶段按业务键兜底定位业务记录并显式写入抄送租户；流程抄送接收人可访问其可见流程表单。
- 签收增加本地任务租户、待办状态、候选用户/候选组成员校验；启动预览优先读取 Flowable UserTask 原生候选集合并使用共享 visited 集合，降低分支流程遍历膨胀。
- BPMN serviceTask 继续只允许 `flowable:type=cc`，并拒绝 raw XML 注入 class/delegateExpression/expression 委托属性。

本轮仍未完成：前后置加签的精确顺序语义、临时抄送关系的流程图/历史聚合展示、稳定游标跨实例进度、完整管理员权限模型、运行时表达式深度解析和真实 MySQL/Redis/Flowable 集成验收。

**协议与权限修复（2026-09-06）**：节点配置读取同时兼容设计器传入的模型 ID 和运行时使用的模型 Key；模型列表/统计查询补充当前租户条件。层级、操作权限、超时和审批人计算入口统一校验节点配置归属，保存、更新、删除和批量保存拒绝跨租户数据。

**Review 修复（2026-09-06）**：模型分页和状态统计此前未向 Mapper 传递租户，`@IgnoreTenant` 控制器下可能返回其他租户模型；已改为显式传入可信租户并在 XML 过滤 `tenant_id` 与未删除状态，新增 SQL 契约断言。

**历史查询性能修复（2026-09-06）**：流程审批时间轴改用租户限定的 `selectHistoryTasks` XML 分页查询，移除 Service 层 `LambdaQueryWrapper`；同一请求内批量读取审批人详情并复用缓存，避免长流程逐任务组织服务回查。现有兼容接口仍限制最多 1000 条，独立分页响应和已执行连线完整计算仍待补齐。

**模型详情权限修复（2026-09-06）**：模型管理控制器使用 `@IgnoreTenant`，原有详情、部署、挂起、激活、启停、导出和复制路径仍可能直接 `getById`；已新增 `selectByIdAndTenant`，所有模型 ID 操作改为当前租户读取，创建/导入/复制强制写入当前租户，分页和统计继续使用显式租户参数。

**超时扫描分页修复（2026-09-06）**：扫描处理会改变活动任务结果集，递增 offset 会跳过后续任务；改为每轮从 offset 0 读取并维护本轮已处理 taskId，通知失败任务不会在同一轮无限重复，自动完成任务也不会被 offset 漏掉。

**通知重试与迁移补强（2026-09-06）**：逾期提醒失败记录新增最多 5 次指数退避重试；通过租户、提醒键、渠道唯一键和原子状态更新避免并发重复发送。新增正式 Flyway `V1.0.144`，补齐提醒记录表、重试字段、节点提醒配置列、重复策略字典和默认消息模板。

**通知租户 fail-closed（2026-09-06）**：逾期提醒不再把缺失租户默认归入租户 1；任务缺少可信 `tenant_id` 时直接跳过并记录告警，避免内部扫描上下文误发到错误租户。

**租户回查与任务镜像写入收敛（2026-09-06）**：

- `FlowTaskServiceImpl` 的终结、历史表单、流程表单详情和业务Key回查全部改为显式租户 Mapper；`@IgnoreTenant` 入口缺少可信租户时 fail-closed。
- `FlowFormInstanceMapper` 新增按流程实例/业务Key的租户限定读取；任务镜像状态更新统一走 `task_id + tenant_id` Mapper，签收、审批、驳回、退回、委派、改派、终结、自动审批和动态加减签不再使用仅按 `task_id` 的隐式更新。
- 流程实例列表、流程实例详情、运行态表单实例和人工抄送参与人回查补齐租户条件，避免旧的无租户业务查询在 Flow 服务边界重新引入越权读取。
- 验证：Flow/Flow Server reactor compile BUILD SUCCESS；插件契约测试 22/22、Flow Server `FlowControllerBoundaryContractTest` 18/18 通过；两个相关 Mapper XML 语法通过。
- 未完成：Flowable 事件监听器/抄送节点等无 HTTP 会话的内部回调仍保留按流程实例的系统回查，需在真实引擎租户上下文中验证；MySQL 执行计划、并发写入和跨租户 HTTP E2E 仍待 T7.2/T7.3。

**继续开发（2026-09-06）**：流程图详情新增连线状态 VO，后端基于 Flowable 历史 `sequenceFlow` 活动批量返回已执行/待执行连线；当历史级别未记录连线活动时返回 `sequenceFlowStatusAvailable=false` 和可读原因，前端流程图查看器显示降级提示，不再把未知状态伪装为未执行。

**继续开发（2026-09-06）**：待办详情和批量审批前端现在为审批、驳回、退回、转办生成 `idempotencyKey + SHA-256 requestDigest`，并透传到 Flow API；服务端重复请求命中已完成结果或返回稳定冲突错误，连续点击与网络重试不会再次推进任务。

**页面可见性补齐（2026-09-06）**：流程设计器候选组选择器同时加载当前租户启用的系统角色和流程用户组，用户组以稳定 `groupCode` 写入 BPMN `candidateGroups`；流程用户组成员维护改用现有租户限定的 `/system/user/page`，修复原来调用不存在 `/api/flow/org/users` 导致成员选择为空的问题。成员响应同时兼容分页 `records`，确保用户组页面可以实际选择成员。

**待办审批详情缺陷修复（2026-09-06）**：`FlowBusinessForm` 动态表单加载增加请求代次和卸载保护，快速切换任务或关闭详情后，旧 loader 结果不会覆盖当前表单或更新已卸载组件；组件结果增加可渲染校验、`markRaw` 和稳定 `key`，避免 Vue 动态组件收到空/失效 vnode 后触发 `emitsOptions` 异常。新增 Vitest 回归覆盖快速切换、卸载结果和找不到表单组件的降级状态。

**模型排序字段缺陷修复（2026-09-06）**：排序更新 SQL 原误写通用字段 `update_by`，而 `sys_flow_model` 的实际审计字段为 `last_update_by`，导致保存排序时报 `Unknown column 'update_by'`；已改为更新 `last_update_by`，并在排序契约测试中断言更新列与表结构一致。

## 本轮继续开发（2026-09-06，用户组公共组件和分页参数修复）

- 用户组页面改用项目公共 `AiCrudPage`、`SystemTableCell`、`DictTag`，统一搜索、表单、表格和操作区样式，移除页面自定义 CRUD 外壳。
- 成员维护改用公共 `UserSelectModal`，复用组织树、分页和租户用户查询；确认时按字符串 ID 计算新增/移除成员，避免雪花 ID 转 Number 造成精度丢失。
- `FlowUserGroupMapper.selectPageByTenant` 增加 `@Param("keyword")`，Service 转发 `FlowUserGroupQuery.keyword`，消除 XML 引用未声明参数导致的 MyBatis `BindingException`。
- 公共选人弹窗增加可选启用状态过滤和允许空选择能力，流程用户组仅展示启用用户，并支持清空全部成员。
- 补充 Mapper 参数契约测试，覆盖 Java 参数声明和 Service 传参。
