# 移动端低代码多端运行时与视觉重构 Tasks

## T1. 建立协议能力基线

- [x] 拉取最新 `origin/main` 并创建独立分支 `codex/mobile-lowcode-runtime-refactor`。
- [x] 盘点管理端统一组件协议与当前移动端实现差集。
- [x] 建立移动端组件能力注册表，覆盖新版设计器、页面画布、旧版设计器组件键和别名。
- [x] 未登记/不安全组件改为明确占位，禁止回退成普通输入框。
- [x] 为注册、别名、平台限制和回退策略补充单元测试。

## T2. 引入 Wot Design Uni 和 Design Tokens

- [x] 安装 Wot Design Uni，并按官方兼容范围固定 Sass 版本。
- [x] 配置 easycom、H5 依赖预构建排除和全局 Wot 主题变量。
- [x] 建立移动端颜色、文字、间距、圆角、表面、边框和安全区令牌。
- [x] 保留 `Ai*` 公共 API，迁移 Button、Field、Select、Popup、Card、Empty、Upload、Signature。
- [x] H5 和微信小程序构建验证。

## T3. 统一字段渲染器

- [x] 将字段类型归一化、展示格式化、校验和选项解析下沉到共享注册器和渲染器。
- [x] 完成文本、数字、金额、单复选、开关、日期时间和范围组件。
- [x] 完成上传、图片、签名和扫码组件。
- [x] 完成人员、组织、区域、级联和关联记录的移动远程选择器，支持静态树、系统接口、受管查询源和安全白名单接口。
- [x] 完成动态数组/明细字段的移动端编辑、校验和只读回显。
- [x] 保持字段联动、数据源、字典和 Long ID 字符串语义。

## T4. 统一布局、页面挂件和业务组件渲染器

- [x] 完成 grid/card/tabs/collapse/space/divider 等移动布局。
- [x] 将 PC 表格、树表、主从页面适配为卡片列表、底部弹层和全屏选择。
- [x] 完成标题、段落、统计、公告、描述、步骤、时间线和基础媒体挂件。
- [x] 完成日历、水印、真实条码/二维码生成和复杂桌面组件的专用移动交互。
- [x] 对动态 Vue、脚本、iframe 和富文本建立安全执行/降级策略。
- [x] 统一查询、分页、工具栏、行操作和底部操作区。

## T5. 普通低代码运行页与独立挂载页

- [x] 抽取移动运行时 store/composables，页面只负责组合视图和业务动作编排。
- [x] 覆盖 list/form/list-form/detail 页面模式和旧配置兼容。
- [x] 应用门户、`app-entry` 和直接链接统一进入同一运行内核。
- [x] 支持页面级导航、标题、返回和安全区配置。
- [x] 将 `lowcode-runtime.vue` 拆分至 800 行以内。

## T6. 审批动态表单

- [x] 普通运行页和审批页共用字段/布局渲染注册器。
- [x] 接入 BPMN 字段权限、主子表 v2 权限和动态数组 v3 权限。
- [x] 覆盖暂存、同意、驳回、退回、委派和必填意见。
- [x] 已办/历史强制只读并展示附件、签名、明细和流程时间线。
- [x] 将 `todo-detail.vue` 拆分至 800 行以内。

## T7. 验证与交付

- [x] 读取自动化测试标准，创建并复用本变更测试基线。
- [x] 运行移动运行时和审批适配器定向单测。
- [x] 运行 H5 与微信小程序生产构建。
- [x] 执行 `git diff --check`，记录执行证据和未完成的真实服务 E2E。
- [x] 固化前后端运行时路由契约测试，并补充 H5/小程序真实联调验收清单。
- [x] 更新组件矩阵、Spec 状态和任务完成情况。

## T8. 审批主子表与提交链路加固

- [x] 默认审批布局自动补齐全部子表区块，显式页面分区遗漏子表时仍安全追加。
- [x] 子表新增、编辑、删除分别遵守 BPMN 节点权限；无权限的持久化行不进入提交载荷。
- [x] 业务对象表单按 `{ main, children }` 提交，业务编码表单保持扁平主表协议，删除行使用 `_deleted` 标记。
- [x] 同意、驳回和指定节点退回通过业务任务动作接口原子保存并办理，避免先保存后办理造成半完成状态。
- [x] 复用业务上下文内嵌的任务表单信息，统一审批要点、意见、签名与动作权限来源。
- [x] 为移动端流程写操作补齐 `idempotencyKey` 与 `requestDigest`，与管理端和既有后端协议一致。
- [x] 新增主子表权限载荷与流程幂等凭证单测，并重新执行双端生产构建。

## T9. H5 全站商务视觉与布局统一

- [x] 收敛全局蓝白黑灰 Design Tokens，移除页面级渐变、光斑、毛玻璃、重阴影和持续装饰动画。
- [x] 重构公共顶部栏、页面容器、区块、列表单元、弹层、标签和底部导航视觉。
- [x] 重构登录、首页、消息、待办、我的、审批详情、低代码运行页和独立入口布局。
- [x] 将登录、首页、消息、我的页面样式拆出，确保生产页面 SFC 均不超过 800 行。
- [x] 完成 H5 页面视觉冒烟、定向单测、H5/微信小程序生产构建与 `git diff --check`。
