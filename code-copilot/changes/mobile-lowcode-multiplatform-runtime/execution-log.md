# 移动端低代码多端运行时执行记录

> 日期：2026-09-21
> 分支：`codex/mobile-lowcode-runtime-refactor`
> 基线：`origin/main@b5690e9f`
> 范围：`forge-h5-ui` 与本变更文档；后端接口、数据库和流程协议未修改。

## 自动化结果

| 检查 | 结果 | 证据 |
|---|---|---|
| API 契约、Pinia 运行时状态、移动注册器、运行时、审批适配器、流程幂等、移动选择器、机器码、uni 请求适配器单测 | 通过 | Node `v24.21.0`，38/38 通过 |
| H5 生产构建 | 通过 | `./node_modules/.bin/uni build`，`DONE Build complete` |
| 微信小程序生产构建 | 通过 | `./node_modules/.bin/uni build -p mp-weixin`，产物位于 `dist/build/mp-weixin` |
| Git 空白错误检查 | 通过 | `git diff --check` 无输出 |
| 分支隔离 | 通过 | 当前分支为 `codex/mobile-lowcode-runtime-refactor`，未在 `main` 上修改 |

微信小程序构建仍报告仓库既有的 API/auth 与 storage 手工分包循环提示，但不影响构建完成；本轮已消除 loading 样式标签选择器告警。

## H5 冒烟验证

- 本地开发页 `http://127.0.0.1:3009/#/` 可打开。
- Wot 适配后的输入框可输入和清空，按钮与登录页布局显示正常。
- 冒烟结束后已停止本地开发服务。
- 控制台仅出现验证码加载失败；原因是本地 App 服务 `127.0.0.1:8583` 未启动，不属于本次前端构建错误。

## 第二轮增量

- 新增统一移动远程选择器，覆盖用户、组织、行政区划、级联、树、业务记录和 `customSelect`。
- 业务记录多选继续序列化为逗号字符串，Snowflake/Long ID 全程按字符串处理。
- 选择结果可同步 `<field>Name`/显式显示字段，并在单选时执行声明式字段映射。
- 动态接口只允许 GET/POST 且路径必须位于 `/system/`、`/ai/`、`/api/`，外部 URL 和危险方法直接阻断。
- 页面可从现有 options/pageSchema 读取移动导航、标题、副标题、返回地址、内容边距和底部安全区配置。

## 第三轮增量

- `lowcode-runtime.vue` 从 1178 行拆分到 791 行，列表、子表、流程轨迹、底部操作、字段事件和流程动作均下沉为独立组件/composable。
- `todo-detail.vue` 从 964 行拆分到 788 行，任务摘要、流程轨迹和页面样式均已拆出。
- 表格、树表和主从明细补充移动卡片实现；远程树和记录选择继续使用底部全屏面板。
- 日历、水印改为移动原生挂件；二维码与 CODE128/CODE39/EAN13/UPC 条形码改为 H5/小程序共用 Canvas 真实生成。
- 新增 `qrcode-generator@1.5.2` 与 `jsbarcode@3.12.3`，版本与管理端现有机器码实现保持一致。

## 第四轮增量

- 新增 `useLowcodeRuntimeStore`，统一管理配置、页面模式、列表分页、搜索条件、主表、子表、字典、流程上下文和页面导航状态；不持久化业务记录，避免跨页面串数据。
- 独立挂载页重新进入时会清空上一页面的列表、查询和表单状态；Snowflake/Long ID 始终保持字符串。
- 新增数据生命周期与表单注册 composable，统一配置加载、字典加载、详情/列表切换、表单引用、主子表校验和卸载清理。
- `lowcode-runtime.vue` 进一步由 791 行降到 544 行，页面保留视图组合与业务动作编排。
- 新增 Pinia 状态测试 3 项，当前定向测试共 30 项。

## 第五轮增量

- 完成移动 API 与后端 Controller 的静态协议核对：发布态配置、动态 CRUD、受管查询源、业务记录选择器、系统选择器、业务任务表单和 Flowable 动作路由均一致。
- 新增 `runtime-api-contract.test.js`，防止后续为 H5/小程序另起 `/mobile`、`/h5` 协议或前后端路由漂移；当前定向测试共 32 项。
- 确认开发代理边界：`/api/flow/**` 与 `/ai/business/flow/**` 走 Flow 服务，其余低代码和系统接口走 App 服务；两个运行服务均装配既有 generator 插件。
- 完成安全静态扫描：未在移动运行时引入 `eval`、`new Function` 或浏览器存储业务记录；动态选项 URL 仅接受同源 `/system/`、`/ai/`、`/api/` 的 GET/POST。
- 新增 `e2e-checklist.md`，覆盖普通 CRUD、独立挂载页、审批动态表单、文件上传、机器码、网关和小程序合法域名的真实环境验收步骤。
- 再次执行 32 项定向测试、H5 构建和微信小程序构建，全部通过。

## 第六轮增量

- 审批默认页面分区现在会包含主表和全部子表；即使设计态分区遗漏某个子表，也会按关系编码补齐，避免可读业务数据在移动端静默消失。
- 子表卡片和编辑弹层分别执行节点级新增、更新、删除权限；持久化删除生成 `{ id, _deleted: true }`，无更新权限的既有行不会被错误提交。
- 业务对象表单继续使用 `{ main, children }` 主子表协议，业务编码表单保持扁平主表协议；字段和值均按可写权限过滤，Snowflake/Long ID 保持字符串。
- 配置化业务表单的同意、驳回和指定节点退回改为通过 `/ai/business/flow/task-action` 原子提交数据与流程动作；手工暂存仍复用现有表单上下文保存接口。
- 业务上下文内嵌的 `taskFormInfo` 现作为审批职责、审批要点、意见/签名要求和动作能力的统一来源，避免重复请求后出现策略漂移。
- 移动端新增与管理端一致的流程幂等凭证生成器；每次用户确认产生新幂等键，同一动作载荷生成稳定请求摘要，网络层重试复用同一个请求体。
- 新增 6 项审批主子表/权限载荷测试和 2 项幂等凭证测试，定向测试提升至 38 项；`todo-detail.vue` 保持 791 行，仍低于 800 行硬限制。

## 第七轮增量：H5 全站商务视觉

- 全局视觉令牌收敛为蓝、白、黑、灰：页面统一灰色工作区、白色业务面板、浅灰边框、深色正文与蓝色主操作；成功、警告、危险色仅用于真实状态。
- 公共顶部栏、页面容器、内容分区、列表单元、表单控件、弹层、通知、结果页、骨架屏、标签和底部导航统一为小圆角、弱装饰、无重阴影的企业移动工作台样式。
- 登录、首页、消息、待办、我的、审批详情、低代码运行页、独立挂载入口和组件演示页完成结构与样式重构；删除装饰网格、渐变、光斑、毛玻璃、悬浮胶囊导航和持续入场动画。
- 登录、首页、消息、待办、我的和组件演示样式拆入 `pages/styles`；页面 SFC 最大文件为 `todo-detail.vue` 791 行，其余生产与演示页面均低于 800 行。
- H5 使用 `390 × 844` 手机视口实际检查登录页与组件目录页：页面层级、密度、分区、选中态和主操作显示正常；弹层多余 `class` 透传警告已修复。
- 本地 H5 开发服务进程 `82470` 已停止。因 App 服务 `127.0.0.1:8583` 未启动，控制台存在验证码和加解密配置请求 500；属于真实后端未启动，不是本轮样式或构建错误。
- 定向测试再次执行，38/38 通过；`pnpm build:h5` 与 `pnpm build:mp-weixin` 均输出 `DONE Build complete`；`git diff --check` 无输出，当前分支仍为 `codex/mobile-lowcode-runtime-refactor`。
- 微信小程序构建继续报告仓库既有 API/auth 与 storage 手工分包循环提示，不阻断产物生成。

## 第八轮增量：统一反馈组件与登录页组件化

- 新增页面级 `AiFeedbackHost`，统一挂载 Wot Design Uni 的 Toast、Notify、MessageBox、Prompt 和 ActionSheet；`utils/notify`、`utils/dialog` 保留原业务调用 API，不再创建手写 DOM，也不再回退 `uni.showToast/showModal/showActionSheet`。
- `pages.json` 注册的 9 个页面均已挂载统一反馈宿主；低代码运行页的记录操作面板、动作确认和动作输入改为统一 Wot 反馈适配器。
- 登录页完成可感知的结构重构：蓝色企业身份区、白色账号面板、`AiField` 账号/密码/验证码、`AiButton` 主操作、Wot Loading 和 `AiPopupSheet` 工作区选择；移除原生输入框与自定义遮罩弹层。
- 新增 `feedback-components.test.js` 4 项契约测试；与既有运行时测试合并执行后共 42/42 通过（Node `v24.21.0`）。
- `pnpm build:h5` 与 `pnpm build:mp-weixin` 均输出 `DONE Build complete`；微信小程序仍仅报告仓库既有 API/auth 与 storage 循环分包警告。
- H5 使用 `390 × 844` 视口实测登录页，页面结构、输入控件、验证码区和主按钮显示完整；点击空表单登录后可见 Wot Toast“请输入用户名、密码和验证码”。
- 静态扫描未发现 `uni.showModal`、`uni.showActionSheet`、`uni.showToast` 或旧 `forge-dialog/forge-toast/forge-notify` 残留；生产页面最大文件 `todo-detail.vue` 为 793 行，`git diff --check` 通过。
- 本轮开发预览服务由 exec 会话启动（Node 子进程日志 PID `294`），验证后已通过 Ctrl+C 停止；端口 `3009` 无监听残留。App 服务 `127.0.0.1:8583` 未启动，验证码请求失败不影响前端反馈组件验证。
- 项目偏好的 Node `v20.19.0` 在当前机器仍未安装，`nvm use v20.19.0` 明确失败；本轮沿用此前构建通过的 Node `v24.21.0`，未将版本切换失败记为通过。

## 第九轮增量：消息与待办处理回路

- 待办页改为每次 `onShow` 都刷新当前作用域，签收、撤回和审批详情返回后不会继续显示旧任务；独立深链办理成功后使用统一回退函数兜底到待办页。
- 流程消息任务 ID 统一从跳转地址、`taskId`、`task_id`、`FLOW_TODO.bizKey` 解析，并始终转为字符串；首页消息深链即使目标不在当前消息页前 80 条中，也会直接请求详情。
- 活动 `FLOW_TODO` 打开详情时不再提前标记已读，“全部已读”只批量处理普通消息；从消息进入审批时携带来源消息 ID，只有动作成功后才调用既有单条已读接口，后端 `FlowTaskNotifyListener` 按 `FLOW_TODO + taskId` 的完成事件继续提供最终一致性兜底。已处理流程消息进入只读审批详情，复用既有历史任务、只读业务表单和流程轨迹协议。
- 审批意见、转办说明、低代码多行文本、数值/金额/数值范围输入改为 `AiTextarea`/`AiField`；`AiSearchBar` 和首页菜单搜索改由 Wot Design Uni `wd-search` 实现。生产页面和组件适配层扫描未发现原生 `input/textarea` 或 `uni.showToast/showModal/showActionSheet` 残留。
- 新增消息导航与处理回路测试，并扩展统一组件契约测试；Node `v24.21.0` 下定向测试共 48/48 通过。
- `pnpm build:h5` 与 `pnpm build:mp-weixin` 均输出 `DONE Build complete`；微信小程序仍仅报告仓库既有 API/auth 与 storage 循环分包提示。
- 390 × 844 H5 视口实际检查组件目录和 Wot 搜索弹层，输入“审批”及清空交互正常，蓝白黑灰布局、边框和焦点态显示完整；为检查临时放开的组件演示访问规则已还原，未形成代码差异。
- 本地预览期间仅出现 App 服务 `127.0.0.1:8583` 未启动导致的请求失败；预览服务和浏览器标签均已关闭。真实消息、待办和审批数据仍按既有约定由联调环境验收。
- 生产页面最大文件为 `todo-detail.vue` 799 行，低于 800 行硬限制；`git diff --check` 通过，分支仍为 `codex/mobile-lowcode-runtime-refactor`。

## 环境差异

仓库记忆推荐 Node `v20.19.0`，但当前机器未安装该版本，因此本次实际使用 Node `v24.21.0`。生产构建和定向测试均已通过；合并前如 CI 固定 Node 20，建议在 CI 再执行同一组命令。

## 第十轮增量：同步 main

- 2026-09-22 将最新 `origin/main@43402b8b` 合并到 `codex/mobile-lowcode-runtime-refactor`；仅 `code-copilot/memory/pitfalls.md` 发生内容冲突，已保留主线与 H5 分支双方条目并更新分类计数。
- 未解决冲突检查无输出，暂存区 `git diff --cached --check` 通过；同时清理主线变更带入的两处空白格式问题。
- H5 定向测试 48/48 通过，`pnpm build:h5` 与 `pnpm build:mp-weixin` 均输出 `DONE Build complete`。
- 微信小程序构建仍仅报告既有 API/auth 与 storage 循环分包提示，不阻断产物生成；本轮未启动任何服务，也未修改数据库。

## 第十一轮增量：火山方舟风格 H5 控制台统一

- 全局 Design Token 收敛为 `#4266F7` 品牌蓝、`#1D2129/#4E5969/#86909C` 文字灰阶、`#C9CDD4` 边框、`#F2F3F5/#F7F8FA` 背景；输入、按钮、卡片和弹层统一 `6px` 圆角，交互控件最小触屏高度统一为 `44px`。
- 公共按钮、输入、文本域、选择器、下划线 Tab、卡片、弹层、页面导航、反馈宿主与底部操作区完成统一；新增 `AiHelpPanel`，用于低代码运行详情的主辅联动说明区域。
- 登录、首页、消息、待办、账户、审批详情、低代码运行页、独立入口和组件演示页完成布局收敛；桌面 H5 在 `1024px` 以上使用主辅分栏或工作区布局，移动端保持单列和满宽主操作。
- 低代码协议、审批表单权限、消息到待办跳转和办理完成回路未改；移动端仍复用发布态配置、动态 CRUD、业务表单与 Flowable 既有协议。
- 新增 `console-design-system.test.js` 5 项规范契约测试；Node `v24.21.0` 下定向测试共 53/53 通过，覆盖运行时、审批、消息、反馈组件与视觉 Token。
- `pnpm build:h5` 与 `pnpm build:mp-weixin` 均输出 `DONE Build complete`；微信小程序仍仅报告仓库既有 API/auth 与 storage 循环分包提示。
- H5 使用 `390 × 844` 和 `1440 × 900` 实际复核登录页：手机端表单、验证码与主按钮完整，桌面端品牌区/登录区双栏和整高背景正常；其他登录态工作区通过结构断言、断点检查与双端构建验证。
- 生产页面最大文件仍为 `todo-detail.vue` 799 行，符合小于 800 行约束；`git diff --check` 通过，当前分支为 `codex/mobile-lowcode-runtime-refactor`。
- 本地预览仅出现 App 服务 `127.0.0.1:8583` 未启动导致的验证码请求失败；预览服务和浏览器标签已关闭，没有修改后端、数据库或认证保护规则。

## 第十二轮增量：首页与待办工作台二次收敛

- `AiField`、`AiSearchBar`、`AiSelect` 和 `AiDateTimePicker` 补齐内部 Flex 对齐、单行内容高度及行高，输入值、占位文字和前后图标在 `44px` 控件内垂直居中，文本仍保持左对齐。
- 首页删除大数字处理卡片墙，收敛为账户导航、工作概览、常用应用和最新提醒四个稳定层级；移动端单列，桌面端使用主工作区与 `320px` 应用侧栏布局。
- 待办筛选区改为下划线工作范围、搜索、流程分类和状态筛选；任务卡固定展示状态、标题、流程节点、申请人、分类、时间和唯一主动作，减少重复按钮与装饰信息。
- 待签收任务点击整卡后先调用既有 `claimFlowTask` 接口，签收成功即进入 `/pages/todo-detail?mode=todo`；已签收任务直接进入处理页，已处理和我发起的记录进入只读页。待办列表已移除 `AiPopupSheet` 中转结构，审批页内实际需要的驳回、转办弹层不受影响。
- `console-design-system.test.js` 增加输入居中、首页层级和待办直达处理页契约；Node `v24.21.0` 下定向测试 56/56 通过。
- `pnpm build:h5` 与 `pnpm build:mp-weixin` 均输出 `DONE Build complete`；微信小程序仍仅报告仓库既有 API/auth 与 storage 循环分包提示。
- 在 `390 × 844` H5 视口检查登录页单行输入，文字、占位符和图标均已垂直居中；登录态首页与待办依靠结构契约、响应式断点和双端生产构建验证，未临时放宽认证保护。
- 本地预览服务和浏览器标签已关闭；App 服务 `127.0.0.1:8583` 未启动导致验证码请求失败，不属于页面构建问题。

## 未执行的真实服务 E2E

没有启动 Admin、App、Flow 服务，也没有写数据库。以下项目需要可用账号、已发布低代码配置、真实待办任务、文件服务及已登记合法域名后由联调环境完成：

- 普通低代码列表、新建、编辑、详情与独立链接跳转；
- 待办保存、同意、驳回、退回、委派以及已办/历史只读；
- H5 与微信小程序真实附件、图片、签名上传；
- 微信开发者工具中的合法域名请求与真机扫码。

## 后续边界

- 动态 Vue/脚本组件仍按安全红线阻断；iframe 在未配置移动端域名白名单时继续明确提示，不直接执行。
- 真实服务 E2E 仍需要已发布低代码配置、流程任务、文件服务和小程序合法域名。
