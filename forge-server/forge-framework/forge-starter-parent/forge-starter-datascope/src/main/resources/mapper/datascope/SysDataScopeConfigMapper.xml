<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdframe.forge.starter.datascope.mapper.SysDataScopeConfigMapper">

    <resultMap id="SysDataScopeConfigResultMap" type="com.mdframe.forge.starter.datascope.entity.SysDataScopeConfig">
        <id property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="resourceCode" column="resource_code"/>
        <result property="resourceName" column="resource_name"/>
        <result property="mapperMethod" column="mapper_method"/>
        <result property="tableAlias" column="table_alias"/>
        <result property="userIdColumn" column="user_id_column"/>
        <result property="orgIdColumn" column="org_id_column"/>
        <result property="tenantIdColumn" column="tenant_id_column"/>
        <result property="regionCodeColumn" column="region_code_column"/>
        <result property="userRegionColumn" column="user_region_column"/>
        <result property="userTableAlias" column="user_table_alias"/>
        <result property="flowRelatedVisible" column="flow_related_visible"/>
        <result property="flowBusinessType" column="flow_business_type"/>
        <result property="recordIdColumn" column="record_id_column"/>
        <result property="enabled" column="enabled"/>
        <result property="remark" column="remark"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="createDept" column="create_dept"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="delFlag" column="del_flag"/>
    </resultMap>

    <sql id="configFilter">
        WHERE del_flag = 0
        <if test="query != null">
            <if test="query.resourceCode != null and query.resourceCode != ''">
                AND resource_code = #{query.resourceCode}
            </if>
            <if test="query.resourceName != null and query.resourceName != ''">
                AND resource_name LIKE CONCAT('%', #{query.resourceName}, '%')
            </if>
            <if test="query.mapperMethod != null and query.mapperMethod != ''">
                AND mapper_method LIKE CONCAT('%', #{query.mapperMethod}, '%')
            </if>
            <if test="query.enabled != null">AND enabled = #{query.enabled}</if>
        </if>
    </sql>

    <select id="selectConfigPage" resultMap="SysDataScopeConfigResultMap">
        SELECT * FROM sys_data_scope_config
        <include refid="configFilter"/>
        ORDER BY create_time DESC, id DESC
    </select>

    <select id="selectConfigList" resultMap="SysDataScopeConfigResultMap">
        SELECT * FROM sys_data_scope_config
        <include refid="configFilter"/>
        ORDER BY create_time DESC, id DESC
    </select>

    <update id="updateConfigStatus">
        UPDATE sys_data_scope_config
        SET enabled = #{enabled}, update_by = #{updateBy}, update_time = CURRENT_TIMESTAMP
        WHERE id = #{id} AND del_flag = 0 AND enabled = #{expectedEnabled}
    </update>

    <select id="selectRuntimeConfigs" resultMap="SysDataScopeConfigResultMap">
        SELECT
            id,
            tenant_id,
            resource_code,
            resource_name,
            mapper_method,
            table_alias,
            user_id_column,
            org_id_column,
            tenant_id_column,
            region_code_column,
            user_region_column,
            user_table_alias,
            flow_related_visible,
            flow_business_type,
            record_id_column,
            enabled,
            remark,
            create_by,
            create_time,
            create_dept,
            update_by,
            update_time,
            del_flag
        FROM sys_data_scope_config
        WHERE del_flag = 0
    </select>

    <select id="selectEnabledModuleConfigs" resultMap="SysDataScopeConfigResultMap">
        SELECT
            id,
            tenant_id,
            resource_code,
            resource_name,
            mapper_method,
            table_alias,
            user_id_column,
            org_id_column,
            tenant_id_column,
            region_code_column,
            user_region_column,
            user_table_alias,
            flow_related_visible,
            flow_business_type,
            record_id_column,
            enabled,
            remark,
            create_by,
            create_time,
            create_dept,
            update_by,
            update_time,
            del_flag
        FROM sys_data_scope_config
        WHERE enabled = 1
          AND del_flag = 0
          AND resource_code IS NOT NULL
          AND resource_code != ''
          AND tenant_id IN (#{tenantId}, 1)
        ORDER BY resource_code ASC,
                 CASE WHEN tenant_id = #{tenantId} THEN 0 ELSE 1 END,
                 id ASC
    </select>

</mapper>
