<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdframe.forge.plugin.generator.mapper.CustomQuerySchemeMapper">

    <resultMap id="BaseResultMap" type="com.mdframe.forge.plugin.generator.domain.entity.CustomQueryScheme">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="config_key" property="configKey"/>
        <result column="scheme_name" property="schemeName"/>
        <result column="conditions_json" property="conditionsJson"/>
        <result column="columns_json" property="columnsJson"/>
        <result column="sort_json" property="sortJson"/>
        <result column="display_json" property="displayJson"/>
        <result column="is_default" property="isDefault"/>
        <result column="del_flag" property="delFlag"/>
        <result column="remark" property="remark"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="create_dept" property="createDept"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Columns">
        id,
        tenant_id,
        config_key,
        scheme_name,
        conditions_json,
        columns_json,
        sort_json,
        display_json,
        is_default,
        del_flag,
        remark,
        create_by,
        create_time,
        create_dept,
        update_by,
        update_time
    </sql>

    <select id="selectUserSchemes" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Columns"/>
        FROM ai_custom_query_scheme
        WHERE tenant_id = #{tenantId}
          AND create_by = #{userId}
          AND config_key = #{configKey}
          AND del_flag = '0'
        ORDER BY is_default DESC, update_time DESC, id DESC
    </select>

    <select id="selectUserScheme" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Columns"/>
        FROM ai_custom_query_scheme
        WHERE tenant_id = #{tenantId}
          AND create_by = #{userId}
          AND config_key = #{configKey}
          AND id = #{id}
          AND del_flag = '0'
        LIMIT 1
    </select>

    <update id="clearDefault">
        UPDATE ai_custom_query_scheme
        SET is_default = 0,
            update_by = #{userId},
            update_time = NOW()
        WHERE tenant_id = #{tenantId}
          AND create_by = #{userId}
          AND config_key = #{configKey}
          AND del_flag = '0'
          AND is_default = 1
    </update>

    <update id="deleteUserScheme">
        UPDATE ai_custom_query_scheme
        SET del_flag = '1',
            update_by = #{userId},
            update_time = NOW()
        WHERE tenant_id = #{tenantId}
          AND create_by = #{userId}
          AND config_key = #{configKey}
          AND id = #{id}
          AND del_flag = '0'
    </update>

</mapper>
