<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdframe.forge.plugin.generator.mapper.BusinessMessageChannelMapper">

    <resultMap id="BaseResultMap" type="com.mdframe.forge.plugin.generator.domain.entity.AiBusinessMessageChannel">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="channel_code" property="channelCode"/>
        <result column="channel_name" property="channelName"/>
        <result column="channel_type" property="channelType"/>
        <result column="channel_config_ref" property="channelConfigRef"/>
        <result column="webhook_url" property="webhookUrl"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="options" property="options"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="create_dept" property="createDept"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Columns">
        id,
        tenant_id,
        channel_code,
        channel_name,
        channel_type,
        channel_config_ref,
        webhook_url,
        description,
        status,
        sort_order,
        options,
        create_by,
        create_time,
        create_dept,
        update_by,
        update_time
    </sql>

    <select id="selectByChannelCode" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Columns"/>
        FROM ai_business_message_channel
        WHERE tenant_id = #{tenantId}
          AND channel_code = #{channelCode}
        LIMIT 1
    </select>

    <select id="selectUserIdsByRoleIds" resultType="java.lang.Long">
        SELECT DISTINCT uor.user_id
        FROM sys_user_org_role uor
        INNER JOIN sys_user u
            ON u.id = uor.user_id
           AND u.user_status = 1
           AND u.del_flag = 0
        INNER JOIN sys_role r
            ON r.id = uor.role_id
           AND r.tenant_id = uor.tenant_id
           AND r.role_status = 1
           AND r.del_flag = 0
        INNER JOIN sys_role_org ro
            ON ro.tenant_id = uor.tenant_id
           AND ro.role_id = uor.role_id
           AND ro.org_id = uor.org_id
        WHERE uor.tenant_id = #{tenantId}
          AND uor.org_id = #{orgId}
          AND uor.role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </select>

    <select id="selectUserIdsByOrgIds" resultType="java.lang.Long">
        SELECT DISTINCT uo.user_id
        FROM sys_user_org uo
        INNER JOIN sys_user u
            ON u.id = uo.user_id
           AND u.tenant_id = uo.tenant_id
           AND u.user_status = 1
           AND u.del_flag = 0
        WHERE uo.tenant_id = #{tenantId}
          AND uo.org_id IN
        <foreach collection="orgIds" item="orgId" open="(" separator="," close=")">
            #{orgId}
        </foreach>
    </select>
</mapper>
