<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdframe.forge.starter.flow.mapper.FlowFormInstanceMapper">

    <select id="selectByProcessInstanceId" resultType="com.mdframe.forge.starter.flow.entity.FlowFormInstance">
        SELECT i.*
        FROM sys_flow_form_instance i
        WHERE i.deleted = 0
          AND i.process_instance_id = #{processInstanceId}
        LIMIT 1
    </select>

    <select id="selectByBusinessKey" resultType="com.mdframe.forge.starter.flow.entity.FlowFormInstance">
        SELECT i.*
        FROM sys_flow_form_instance i
        WHERE i.deleted = 0
          AND i.business_key = #{businessKey}
        LIMIT 1
    </select>

    <update id="updateProcessInstance">
        UPDATE sys_flow_form_instance
        SET process_instance_id = #{processInstanceId},
            status = #{status},
            update_time = NOW()
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <update id="updateStatusByProcessInstanceId">
        UPDATE sys_flow_form_instance
        SET status = #{status},
            end_time = NOW(),
            update_time = NOW()
        WHERE process_instance_id = #{processInstanceId}
          AND tenant_id = #{tenantId}
          AND deleted = 0
    </update>

    <update id="deleteByProcessInstanceIdLogically">
        UPDATE sys_flow_form_instance
        SET deleted = 1,
            update_time = NOW()
        WHERE process_instance_id = #{processInstanceId}
          AND tenant_id = #{tenantId}
          AND deleted = 0
    </update>
</mapper>
